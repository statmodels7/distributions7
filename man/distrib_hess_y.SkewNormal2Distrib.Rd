% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skewnormal2_distrib.R
\name{distrib_hess_y.SkewNormal2Distrib}
\alias{distrib_hess_y.SkewNormal2Distrib}
\title{Skew Normal Second Response Derivative in the Centered Parametrization}
\arguments{
\item{distrib}{A \code{SkewNormal2Distrib} object, from \code{\link[=skewnormal2_distrib]{skewnormal2_distrib()}}.}

\item{y}{A numeric vector of observations.}

\item{theta}{A named list with components \code{mu}, \code{sigma} and \code{gamma1}, each a
numeric vector of length 1 or of the length of \code{y}.}

\item{...}{Passed to \code{\link[=distrib_hess_y.SkewNormal1Distrib]{distrib_hess_y.SkewNormal1Distrib()}}.}
}
\value{
A numeric vector of the length of the recycled inputs, negative
throughout.
}
\description{
Computes \eqn{\partial^2\ell/\partial y^2} by delegating to
\code{\link[=distrib_hess_y.SkewNormal1Distrib]{distrib_hess_y.SkewNormal1Distrib()}} at the implied direct parameters. Like
the first response derivative it is the parent's unchanged, and is defined
at \eqn{\gamma_1 = 0}.

The value is strictly negative at every observation and every skewness: the
skew normal log-density is concave in the response.
}
\examples{
d <- skewnormal2_distrib()
y <- c(-1, 0.3, 1.7)
th <- list(mu = 0, sigma = 1, gamma1 = 0.5)

# Against a central difference of the response derivative.
eps <- 1e-5
rbind(analytic = distrib_hess_y(d, y, th),
      numeric = (distrib_grad_y(d, y + eps, th) -
                 distrib_grad_y(d, y - eps, th)) / (2 * eps))

# Concave in the response at every skewness the family reaches.
vapply(c(-0.9, -0.3, 0.3, 0.9), function(g)
  max(distrib_hess_y(d, seq(-6, 6, by = 0.5),
                     list(mu = 0, sigma = 1, gamma1 = g))), 0)
}
\seealso{
\code{\link[=distrib_grad_y.SkewNormal2Distrib]{distrib_grad_y.SkewNormal2Distrib()}} for the first derivative,
\code{\link[=distrib_hess_y.SkewNormal1Distrib]{distrib_hess_y.SkewNormal1Distrib()}} for the closed form, and
\code{\link[=distrib_hess_y]{distrib_hess_y()}} for the generic.
}
