% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/laplace2_distrib.R
\name{kink_decomposition.Laplace2Distrib}
\alias{kink_decomposition.Laplace2Distrib}
\title{The Rate-Parametrized Laplace's Kink}
\arguments{
\item{distrib}{A \code{Laplace2Distrib} object.}
}
\value{
A \code{\link[=kink_spec]{kink_spec()}} with \code{phi = "abs"}.
}
\description{
The log-density is \eqn{\log(\lambda/2) - \lambda \lvert y - \mu \rvert}, so
the composition is the absolute value at \eqn{v = y - \mu} with
\eqn{c(\theta) = -\lambda}.
}
\details{
The same kink as \code{\link[=laplace_distrib]{laplace_distrib()}}'s, carried across the map
\eqn{\lambda = 1/\sigma}: a reparametrization moves the coefficient in front
and leaves the argument alone, so the order is unchanged and the location is
again the only non-smooth parameter.
}
\examples{
kink_decomposition(laplace2_distrib())
params_order(laplace2_distrib())

}
\seealso{
\code{\link[=kink_decomposition]{kink_decomposition()}} for the generic, \code{\link[=laplace2_distrib]{laplace2_distrib()}} for
the family.
}
\keyword{internal}
