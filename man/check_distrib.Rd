% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_distrib.R
\name{check_distrib}
\alias{check_distrib}
\title{Numerically Validate a Distribution}
\usage{
check_distrib(
  distrib,
  theta = NULL,
  n = 100,
  nsim = 2e+05,
  orders = 1:4,
  tol = 0.001,
  verbose = TRUE
)
}
\arguments{
\item{distrib}{An object inheriting from class \code{"distrib"}.}

\item{theta}{A named list of parameter values at which to run the checks. If
\code{NULL} (default) a random admissible value is drawn with
\code{\link[=generate_random_theta]{generate_random_theta()}}.}

\item{n}{Integer. Number of observations used for the derivative comparisons.
Defaults to 100.}

\item{nsim}{Integer. Monte Carlo sample size used for the random generator and
expected-information checks. Defaults to 200000.}

\item{orders}{Integer vector. Which parameter-derivative orders to check.
Defaults to \code{1:4}, the orders every family implements analytically; use
e.g. \code{1:2} for a faster run. Add \code{5} to check the numerical fifth as
well; see the bullet below for what that row compares and when it is
emitted.}

\item{tol}{Numeric. Relative tolerance for the finite-difference comparisons.
Defaults to \code{1e-3}.}

\item{verbose}{Logical. If \code{TRUE} (default) a readable report is printed.}
}
\value{
Invisibly, a \code{data.frame} with one row per check and columns
\code{check}, \code{status} (\code{"OK"} or \code{"FAIL"}), \code{statistic}
and \code{detail}. A check whose statistic came out \code{NaN} or \code{NA} is not a row:
it is listed in the attribute \code{"skipped"}, a data frame with columns
\code{check} and \code{reason}, which is present only when some check was skipped.
}
\description{
Runs a battery of numerical self-consistency checks on a \code{distrib} object.
Validates a user-defined distribution: it verifies that the density integrates (or sums) to one, that the
CDF, quantile function and random generator agree with each other and with the
density, and that every analytical derivative matches its finite-difference
counterpart.
}
\details{
The checks performed are:
\itemize{
\item \strong{density}: non-negativity and integration/summation to 1 over the support.
\item \strong{cdf}: values in \eqn{[0,1]} and monotonicity along a grid of quantiles,
and its agreement with the density: for a continuous family a central
difference of the cdf against the density, with the step the response
derivatives use, and for a discrete one \eqn{F(k) - F(k-1)} against the
mass.
\item \strong{quantile}: round-trip against the CDF (\eqn{F(Q(p)) = p} for continuous
distributions, and the generalized-inverse inequalities for discrete ones).
\item \strong{rng}: the sample mean and variance of a large draw agree with
\code{\link[=mean]{mean()}} and \code{\link[=variance]{variance()}} within Monte Carlo error.
\item \strong{gradient, hessian, deriv3, deriv4}: analytical values against
\code{\link[=numerical_gradient]{numerical_gradient()}}, \code{\link[=numerical_hessian]{numerical_hessian()}},
\code{\link[=numerical_deriv3]{numerical_deriv3()}} and \code{\link[=numerical_deriv4]{numerical_deriv4()}}.
\item \strong{deriv5}, when \code{5} is among \code{orders}: \code{\link[=distrib_deriv5]{distrib_deriv5()}} against
\code{\link[=numerical_deriv5]{numerical_deriv5()}} at a higher accuracy, five stencil nodes instead of
three. No family writes the fifth order out, so there is no analytic value
to compare against and this checks the differencing rather than a family's
algebra. It is emitted only where \code{\link[=has_exact_deriv4]{has_exact_deriv4()}} is \code{TRUE}: where the
fourth order is itself a fallback the fifth is a difference of a difference
and no verdict on it would mean anything. A family that owns its
fourth-order method while building part of it from stencils passes that
test and may still fail this row: \code{\link[=skewt_distrib]{skewt_distrib()}} fails it at \code{nu = 3}
and passes from \code{nu = 8} upward, the noise read here being absolute and
falling as \code{nu} grows. It is why \code{orders} defaults to \code{1:4}.
\item \strong{expected information}: \code{\link[=distrib_expected_hessian]{distrib_expected_hessian()}} against a
Monte Carlo estimate of \eqn{-\mathbb{E}[\nabla\ell\,\nabla\ell^\top]}. The outer
product of the score is used as reference because it remains valid when the
log-likelihood is not differentiable in a parameter (see \code{\link[=laplace_distrib]{laplace_distrib()}}).
A draw landing exactly on a finite bound of a continuous support, where the
score of a family singular at that bound is infinite, is left out of the
estimate and counted in the row's \code{detail}; a score that is not finite at
an interior point still leaves the row without a statistic.
\item \strong{response derivatives} (continuous only): \code{\link[=distrib_grad_y]{distrib_grad_y()}} and
\code{\link[=distrib_hess_y]{distrib_hess_y()}} against central differences in \eqn{y} whose step is
chosen observation by observation by \code{\link[=fd_stable_quotient]{fd_stable_quotient()}}, between a
step cut to under half the distance to a bound and one scaled on that
distance, each divided by the steps its evaluation points actually lie
at. A draw closer than \eqn{128\lvert b\rvert\varepsilon} to a finite
bound \eqn{b \ne 0}, where the spacing of doubles is absolute and no
central difference compares a derivative, is left out and counted in the
row's \code{detail}.
\item \strong{link scale}: \code{scale = "link"} derivatives against finite
differences of the log-likelihood in \eqn{\eta}.
}

Distributions that rely on the numerical fallbacks pass the corresponding
parameter-derivative checks trivially, since analytical and numerical values
then coincide by construction. The response fallbacks take the cut step
alone, so near a bound they can differ from the reference, which is what the
row then reports.

A check whose statistic comes out \code{NaN} or \code{NA} has nothing to judge, as the
expected information of a family where it does not exist, and is not a row
of the table: it is listed with its reason in the attribute \code{"skipped"}, and
every row that is in the table reads \code{"OK"} or \code{"FAIL"}. An infinite
statistic is a failure, being a component that overflows where its reference
does not; so is a check whose computation raised an error, and so is a
density or a distribution function that is not finite, those three checks
being defined on the values themselves.

Mixed distributions --- a density with point masses on top of it, as produced by
\code{\link[=zero_adjusted]{zero_adjusted()}} on a continuous parent --- are handled as long as they
declare their atoms through \code{\link[=distrib_atoms]{distrib_atoms()}}. The density is then expected
to integrate to one minus the atomic mass, quantiles falling inside a jump of the CDF
are checked as generalized inverses rather than exact ones, and finite differences in
\eqn{y} are kept away from the atoms, where no derivative exists.
}
\examples{
\dontrun{
check_distrib(gaussian1_distrib())
check_distrib(laplace_distrib(), theta = list(mu = 1, sigma = 2))
check_distrib(poisson_distrib(), orders = 1:2, nsim = 5e4)
}

}
\seealso{
\code{\link[=numerical_gradient]{numerical_gradient()}}, \code{\link[=numerical_hessian]{numerical_hessian()}},
\code{\link[=link_scale_derivatives]{link_scale_derivatives()}}
}
