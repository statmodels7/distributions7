% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/student_t1_distrib.R
\name{StudentT1Distrib}
\alias{StudentT1Distrib}
\title{Student t Distribution Class, Location, Scale and Degrees of Freedom}
\usage{
StudentT1Distrib(
  distrib_name = character(0),
  dimension = character(0),
  bounds = integer(0),
  params = character(0),
  params_interpretation = character(0),
  n_params = integer(0),
  params_bounds = list(),
  link_params = list(),
  params_smooth = logical(0)
)
}
\arguments{
\item{distrib_name}{A single character string specifying the name of the distribution (e.g., \code{"student t"}).}

\item{dimension}{A character string indicating the dimensionality (\code{"univariate"} or \code{"multivariate"}).}

\item{bounds}{A numeric vector of length 2 defining the overall support of the distribution \code{c(lower, upper)}.}

\item{params}{A character vector containing the names of the distribution parameters (e.g., \code{c("mu", "sigma")}).}

\item{params_interpretation}{A character vector (typically named) providing the statistical interpretation of each parameter (e.g., \code{c(mu = "location")}).}

\item{n_params}{A numeric value specifying the total number of parameters.}

\item{params_bounds}{A list of numeric vectors of length 2, specifying the valid mathematical domain for each individual parameter.}

\item{link_params}{A list of link function objects corresponding to each parameter, primarily used to map parameters to the unconstrained real line for optimization algorithms.}

\item{params_smooth}{An optional named logical vector flagging, for each parameter, whether the log-likelihood is differentiable with respect to it. Defaults to all \code{TRUE} (leave empty). Set an entry to \code{FALSE} for parameters at which the log-likelihood has a kink (e.g. the location of a Laplace distribution): the observed Hessian is then degenerate and the expected information must be obtained from the score variance rather than from \eqn{-\mathbb{E}[H]} (see \code{\link[=distrib_expected_hessian]{distrib_expected_hessian()}}).}
}
\value{
An S7 object of class \code{StudentT1Distrib}, inheriting from
\code{continuous_distrib} and from \code{distrib}. Its properties are the parent's:
\code{distrib_name}, \code{dimension}, \code{bounds}, \code{params}, \code{params_interpretation},
\code{n_params}, \code{params_bounds}, \code{link_params} and \code{params_smooth}. For an
object built by \code{\link[=student_t1_distrib]{student_t1_distrib()}} they hold \code{"student t1"},
\code{"univariate"}, \code{c(-Inf, Inf)}, \code{c("mu", "sigma", "nu")}, the
interpretations \code{c(mu = "location", sigma = "scale", nu = "shape")}, \code{3},
the domains \eqn{(-\infty, \infty)} and \eqn{(0, \infty)} twice, and the
three links.
}
\description{
The S7 class of the location-scale Student t family, parametrized by a
location \eqn{\mu}, a scale \eqn{\sigma > 0} and degrees of freedom
\eqn{\nu > 0}, with density proportional to
\eqn{\{1 + (y-\mu)^2/(\nu\sigma^2)\}^{-(\nu+1)/2}} on the whole real line.
It inherits from \code{continuous_distrib}, so it answers every generic of the
\code{distrib} contract; the eleven methods listed below are registered on it
directly and everything else comes from the parent.

Build one with \code{\link[=student_t1_distrib]{student_t1_distrib()}}, which supplies the three link
functions and fills the properties in. This page documents the raw S7
constructor, which takes the parent's properties and validates none of the
relationships between them.
}
\section{Methods}{

Registered on this class in this file:
\code{\link[=distrib_cdf.StudentT1Distrib]{distrib_cdf()}},
\code{\link[=distrib_deriv3.StudentT1Distrib]{distrib_deriv3()}},
\code{\link[=distrib_deriv4.StudentT1Distrib]{distrib_deriv4()}},
\code{\link[=distrib_expected_hessian.StudentT1Distrib]{distrib_expected_hessian()}},
\code{\link[=distrib_grad_y.StudentT1Distrib]{distrib_grad_y()}},
\code{\link[=distrib_gradient.StudentT1Distrib]{distrib_gradient()}},
\code{\link[=distrib_hess_y.StudentT1Distrib]{distrib_hess_y()}},
\code{\link[=distrib_hessian.StudentT1Distrib]{distrib_hessian()}},
\code{\link[=distrib_pdf.StudentT1Distrib]{distrib_pdf()}},
\code{\link[=distrib_quantile.StudentT1Distrib]{distrib_quantile()}},
\code{\link[=distrib_rng.StudentT1Distrib]{distrib_rng()}}.

Nine more are registered on the class from other files: the mixed
derivatives \code{\link[=distrib_cross_y.StudentT1Distrib]{distrib_cross_y()}} and
\code{\link[=distrib_cross2_y.StudentT1Distrib]{distrib_cross2_y()}}, the
distribution-function derivatives
\code{\link[=distrib_grad_cdf.StudentT1Distrib]{distrib_grad_cdf()}} and
\code{\link[=distrib_hess_cdf.StudentT1Distrib]{distrib_hess_cdf()}},
\code{distrib_grad_y_hess()} and \code{distrib_hess_y_hess()} in
\code{theta2_families.R}, and the four moments
\code{\link[=mean.StudentT1Distrib]{mean()}}, \code{\link[=variance.StudentT1Distrib]{variance()}},
\code{\link[=skewness.StudentT1Distrib]{skewness()}} and
\code{\link[=kurtosis.StudentT1Distrib]{kurtosis()}} in \code{moments.R}.

Everything else is inherited from \code{\link[=continuous_distrib]{continuous_distrib()}}.
}

\examples{
d <- student_t1_distrib()
S7::S7_inherits(d, continuous_distrib)

# The properties a consumer reads to drive the family without knowing it.
d@params
d@params_interpretation
d@params_bounds

# The location is already free; the scale and the degrees of freedom ride
# a log, both being positive.
vapply(d@link_params, function(l) l@link_name, character(1))
}
\seealso{
\code{\link[=student_t1_distrib]{student_t1_distrib()}} to build one;
\code{\link[=student_t2_distrib]{student_t2_distrib()}} for the same law parametrized by the standard
deviation; \code{\link[=gaussian1_distrib]{gaussian1_distrib()}} for the limit at \eqn{\nu \to \infty};
\code{\link[=cauchy_distrib]{cauchy_distrib()}} for the case \eqn{\nu = 1};
\code{\link[=distrib_gradient.StudentT1Distrib]{distrib_gradient.StudentT1Distrib()}} for the redescending score.
}
