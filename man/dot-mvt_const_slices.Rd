% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mvstudent_t_distrib.R
\name{.mvt_const_slices}
\alias{.mvt_const_slices}
\title{Repeat a Constant Matrix Across Slices}
\usage{
.mvt_const_slices(m, v)
}
\arguments{
\item{m}{A \eqn{p \times p} numeric matrix.}

\item{v}{A numeric vector of length \eqn{n}, which sets the third dimension
of the result.}
}
\value{
A \eqn{p \times p \times n} numeric array, with \code{n = length(v)}.
}
\description{
Builds the \eqn{p \times p \times n} array whose \eqn{i}th slice is
\code{v[i] * m}, the contribution of a term with a fixed matrix and a
per-observation scalar. It is the mirror of
\code{\link[=.mvt_scale_slices]{.mvt_scale_slices()}}, where the array varies and the scalar is applied to
it; here the matrix is the same everywhere and only the scalar moves.
}
\examples{
m <- matrix(c(1, 2, 2, 4), 2, 2)
cs <- distributions7:::.mvt_const_slices(m, c(1, -1))
dim(cs)
cs[, , 2]

# Every slice is a multiple of the one matrix.
all.equal(cs[, , 1], m)
all.equal(cs[, , 2], -m)

}
\seealso{
\code{\link[=.mvt_scale_slices]{.mvt_scale_slices()}} for the mirror case and
\code{\link[=distrib_hess_y_hess.MvStudentTDistrib]{distrib_hess_y_hess.MvStudentTDistrib()}} for the consumer.
}
\keyword{internal}
