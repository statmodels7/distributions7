% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{mean.CauchyDistrib}
\alias{mean.CauchyDistrib}
\title{The Cauchy Distribution Has No Mean}
\arguments{
\item{x}{A \code{CauchyDistrib}, from \code{\link[=cauchy_distrib]{cauchy_distrib()}}.}

\item{theta}{A named list with components \code{mu} and \code{sigma} (positive), each
a numeric vector of length 1 or \code{n}. The values are not read, only their
lengths, and the list is still aligned and validated, so a missing or
out-of-bounds component throws as it would for a family whose moments
exist.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of \code{NaN}, of length
\code{max(length(theta$mu), length(theta$sigma))}.
}
\description{
Returns \code{NaN}. The Cauchy density decays like \eqn{y^{-2}}, so
\eqn{\int |y| f(y)\,\mathrm{d}y} diverges and the first moment does not
exist. \code{NaN} is returned directly, so that no quadrature is attempted: a
numerical integration over a divergent integral returns whatever its
truncation gives, a number that moves with the panel layout and reads like
an estimate.
}
\details{
What the family does have is a \strong{median}, equal to \eqn{\mu}, and a
half-interquartile range, equal to \eqn{\sigma}. Both are exact at every
parameter value and both come from \code{\link[=distrib_quantile]{distrib_quantile()}}. A Cauchy sample
mean is itself Cauchy with the same scale, so it does not settle as the
sample grows and cannot stand in for the missing moment either.
}
\section{Notation}{

\eqn{\mu} is the location and \eqn{\sigma > 0} the scale, in the
parametrization \code{\link[=cauchy_distrib]{cauchy_distrib()}} uses. Neither is a moment.
}

\examples{
d <- cauchy_distrib()

# No moment exists, so this is NaN at every parameter value.
mean(d, list(mu = c(0, 1, 2), sigma = 1))

# The median is mu and the half-interquartile range is sigma.
distrib_quantile(d, c(0.25, 0.5, 0.75), list(mu = 3, sigma = 2))

}
\seealso{
\code{\link[=skewness.CauchyDistrib]{skewness.CauchyDistrib()}} for the full argument;
\code{\link[=variance.CauchyDistrib]{variance.CauchyDistrib()}} and \code{\link[=kurtosis.CauchyDistrib]{kurtosis.CauchyDistrib()}}, \code{NaN} for the
same reason; \code{\link[=distrib_quantile]{distrib_quantile()}} for the median and the quartiles.
}
\keyword{internal}
