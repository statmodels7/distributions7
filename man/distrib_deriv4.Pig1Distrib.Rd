% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pig1_distrib.R
\name{distrib_deriv4.Pig1Distrib}
\alias{distrib_deriv4.Pig1Distrib}
\title{Poisson-Inverse Gaussian Fourth Derivatives}
\arguments{
\item{distrib}{A \code{Pig1Distrib} object, from \code{\link[=pig1_distrib]{pig1_distrib()}}.}

\item{y}{A numeric vector of counts. With \code{expected = TRUE} its values are
the support points the expectation is summed over.}

\item{theta}{A named list with components \code{mu} and \code{sigma}, each a numeric
vector of length 1 or of the length of \code{y}.}

\item{expected}{Logical of length 1. When \code{FALSE}, the default, the observed
derivatives at \code{y} are returned from the compiled kernel.}

\item{scale}{Either \code{"parameter"}, the default, or \code{"link"}. The
transformation is applied in the generic's body.}

\item{approx}{One of \code{"integrate"}, \code{"bartlett"}, \code{"mc"} or \code{"opg"}, read
only when \code{expected = TRUE}.}

\item{nsim}{A single positive integer, the Monte Carlo sample size used when
\code{approx = "mc"}. Defaults to \code{10000}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}

\item{threads}{A single positive integer, how many threads the compiled
kernel may use. Defaults to \code{1L}.}
}
\value{
A named list of five numeric vectors: \code{mu_mu_mu_mu},
\code{mu_mu_mu_sigma}, \code{mu_mu_sigma_sigma}, \code{mu_sigma_sigma_sigma} and
\code{sigma_sigma_sigma_sigma}.
}
\description{
Returns the exact fourth derivatives of the log-mass in
\eqn{(\mu, \sigma)}, read off the last five columns of the compiled kernel
of \code{pig1_deriv4_cpp}. The kernel is asked for this order alone, so this is
the order it was written for and it costs the same as the score.

With \code{expected = TRUE} the value is an expectation and is not closed form,
as at third order.
}
\examples{
d <- pig1_distrib()
y <- 0:6
th <- list(mu = 3, sigma = 0.8)
d4 <- distrib_deriv4(d, y, th)
names(d4)

# Against a central difference of the third order.
eps <- 1e-5
rbind(analytic = d4$mu_mu_mu_sigma,
      numeric = (distrib_deriv3(d, y, list(mu = 3, sigma = 0.8 + eps))$mu_mu_mu -
                 distrib_deriv3(d, y, list(mu = 3, sigma = 0.8 - eps))$mu_mu_mu) /
                (2 * eps))

# All four orders come from one pass of the kernel, so the fourth costs
# what the first does.
n <- 2e4
set.seed(62)
x <- distrib_rng(d, n, th)
rbind(score = system.time(distrib_gradient(d, x, th))[["elapsed"]],
      fourth = system.time(distrib_deriv4(d, x, th))[["elapsed"]])
}
\seealso{
\code{\link[=distrib_deriv3.Pig1Distrib]{distrib_deriv3.Pig1Distrib()}} for the order below,
\code{pig1_deriv4_cpp} for the kernel, and \code{\link[=distrib_deriv4]{distrib_deriv4()}} for the generic.
}
