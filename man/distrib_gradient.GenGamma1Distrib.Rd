% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gengamma1_distrib.R
\name{distrib_gradient.GenGamma1Distrib}
\alias{distrib_gradient.GenGamma1Distrib}
\title{Generalized Gamma Score}
\arguments{
\item{distrib}{A \code{GenGamma1Distrib} object, from \code{\link[=gengamma1_distrib]{gengamma1_distrib()}}.}

\item{y}{A numeric vector of observations, strictly positive.}

\item{theta}{A named list with components \code{a}, \code{d} and \code{p}, each a numeric
vector of length 1 or of the length of \code{y}. All three must be strictly
positive.}

\item{scale}{Either \code{"parameter"}, the default, or \code{"link"}. The
transformation to the link scale is applied in the generic's body, so this
method always returns the parameter scale.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}

\item{threads}{A single positive integer, how many threads the compiled
kernel may use. Defaults to \code{1L}. The result does not depend on the count.}
}
\value{
A named list of three numeric vectors, \code{a}, \code{d} and \code{p}, each of the
length of the recycled inputs.
}
\description{
Computes the three first derivatives of the log-density in closed form, in a
compiled kernel. With \eqn{w = (y/a)^{p}}, \eqn{L = \log(y/a)} and
\eqn{k = d/p},
\deqn{\dfrac{\partial\ell}{\partial a} = \dfrac{pw - d}{a}, \qquad
      \dfrac{\partial\ell}{\partial d} = L - \dfrac{\psi(k)}{p}, \qquad
      \dfrac{\partial\ell}{\partial p} = \dfrac{1}{p}
        + \dfrac{d\,\psi(k)}{p^{2}} - wL.}
Only the digamma function appears beyond elementary operations, and it
appears at one argument, \eqn{k = d/p}.
}
\section{Notation}{

\eqn{\ell} is the log-density of one observation, \eqn{\psi} the digamma
function \code{digamma()}, and \eqn{a}, \eqn{d}, \eqn{p} the scale and the two
shapes.
}

\examples{
d <- gengamma1_distrib()
y <- c(0.5, 1.5, 4)
th <- list(a = 2, d = 3, p = 1.5)
g <- distrib_gradient(d, y, th)

# The scale component written out.
all.equal(g$a, (1.5 * (y / 2)^1.5 - 3) / 2)

# All three against numerical differentiation of the log-likelihood.
f <- function(v) sum(distrib_pdf(d, y, list(a = v[1], d = v[2], p = v[3]),
                                 log = TRUE))
rbind(analytic = vapply(g, sum, 0),
      numeric = numDeriv::grad(f, c(2, 3, 1.5)))

# The score sums to nearly zero at the maximum likelihood estimate.
set.seed(52)
x <- distrib_rng(d, 2000, th)
fit <- fit_distrib(d, x)
vapply(distrib_gradient(d, x, as.list(coef(fit))), sum, 0) / 2000
}
\seealso{
\code{\link[=distrib_hessian.GenGamma1Distrib]{distrib_hessian.GenGamma1Distrib()}} for the second derivatives,
\code{\link[=distrib_expected_hessian.GenGamma1Distrib]{distrib_expected_hessian.GenGamma1Distrib()}} for the closed-form
information, and \code{\link[=distrib_gradient]{distrib_gradient()}} for the generic.
}
