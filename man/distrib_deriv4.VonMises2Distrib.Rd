% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vonmises2_distrib.R
\name{distrib_deriv4.VonMises2Distrib}
\alias{distrib_deriv4.VonMises2Distrib}
\title{von Mises Fourth-Order Derivatives in the Resultant Length}
\arguments{
\item{distrib}{A \code{VonMises2Distrib} object, from \code{\link[=vonmises2_distrib]{vonmises2_distrib()}}.}

\item{y}{A numeric vector of angles in \eqn{[-\pi, \pi)}. With
\code{expected = TRUE} only its length is read.}

\item{theta}{A named list with components \code{mu} and \code{rho}, each a numeric
vector of length 1 or of the length of \code{y}. \code{rho} must lie in
\eqn{(0, 1)}.}

\item{scale}{One of \code{"parameter"} (the default) or \code{"link"}, matched by
\code{\link[base:match.arg]{base::match.arg()}}. Read by the generic, not by this method. \strong{Note the
argument order}: \code{scale} precedes \code{expected} here, so both are best given
by name.}

\item{expected}{Logical of length 1. When \code{TRUE} the expectation under the
model is returned in place of the value at the data, computed numerically.
Defaults to \code{FALSE}.}

\item{approx}{One of \code{"integrate"} (the default here), \code{"bartlett"}, \code{"mc"}
or \code{"opg"}. Read only when \code{expected = TRUE}.}

\item{nsim}{A single positive integer, the sample size when
\code{approx = "mc"}. Read only when \code{expected = TRUE}. Defaults to \code{10000}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A named list of five numeric vectors, \code{mu_mu_mu_mu},
\code{mu_mu_mu_rho}, \code{mu_mu_rho_rho}, \code{mu_rho_rho_rho} and \code{rho_rho_rho_rho},
each of length \code{length(y)}.
}
\description{
Computes the five distinct fourth derivatives of the log-density in
\eqn{\mu} and \eqn{\rho}, in closed form, by the construction
\code{\link[=distrib_deriv3.VonMises2Distrib]{distrib_deriv3.VonMises2Distrib()}} describes carried one order further: a
single term \eqn{D_a \kappa^{(b)}(\rho)} for every component carrying a
\eqn{\mu}, and the fourth-order one-variable Faa di Bruno on
\eqn{\log I_0} for the pure-\eqn{\rho} one.

With \code{expected = TRUE} the method calls \code{\link[=expected_derivative]{expected_derivative()}}, which is
the one place on this page where \code{approx} and \code{nsim} are read.
}
\section{Notation}{

\eqn{\ell} is the log-density of one observation, \eqn{\mu} the mean
direction, \eqn{\rho \in (0,1)} the mean resultant length, \eqn{\kappa} the
concentration and \eqn{A(\kappa) = I_1(\kappa)/I_0(\kappa)}.
}

\examples{
d2 <- vonmises2_distrib()
y <- c(-1, 0, 0.5, 2)
th <- list(mu = 0.5, rho = 0.7)
d4 <- distrib_deriv4(d2, y, th)
names(d4)

# A central difference of the third order reproduces the pure-direction
# component.
eps <- 1e-5
up <- distrib_deriv3(d2, y, list(mu = 0.5 + eps, rho = 0.7))$mu_mu_mu
dn <- distrib_deriv3(d2, y, list(mu = 0.5 - eps, rho = 0.7))$mu_mu_mu
all.equal((up - dn) / (2 * eps), d4$mu_mu_mu_mu, tolerance = 1e-5)
}
\seealso{
\code{\link[=distrib_deriv3.VonMises2Distrib]{distrib_deriv3.VonMises2Distrib()}} for the order below and the
construction, \code{\link[=vm2_parts]{vm2_parts()}} for the map's derivatives, and
\code{\link[=distrib_deriv4]{distrib_deriv4()}} for the generic.
}
