% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{skewness.LaplaceDistrib}
\alias{skewness.LaplaceDistrib}
\title{Skewness of the Laplace Distribution}
\arguments{
\item{x}{A \code{LaplaceDistrib}, from \code{\link[=laplace_distrib]{laplace_distrib()}}.}

\item{theta}{A named list with components \code{mu} and \code{sigma}, each a numeric
vector of length 1 or \code{n}. The values are not read, only their lengths,
and the list is still aligned and validated.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of zeros, of length
\code{max(length(theta$mu), length(theta$sigma))}.
}
\description{
Exactly zero at every parameter value. The density is symmetric about
\eqn{\mu}, so every odd central moment vanishes. The constant is returned
directly, recycled to the length the parameters imply.
}
\examples{
d <- laplace_distrib()

# Zero at every location and scale, with one value per setting.
skewness(d, list(mu = c(0, 5), sigma = c(1, 2)))

}
\seealso{
\code{\link[=kurtosis.LaplaceDistrib]{kurtosis.LaplaceDistrib()}}, which is 3;
\code{\link[=mean.LaplaceDistrib]{mean.LaplaceDistrib()}}; \code{\link[=laplace_distrib]{laplace_distrib()}}.
}
\keyword{internal}
