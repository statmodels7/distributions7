% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{kurtosis.Pig2Distrib}
\alias{kurtosis.Pig2Distrib}
\title{Excess Kurtosis of the Orthogonal Poisson-Inverse Gaussian Distribution}
\arguments{
\item{x}{A \code{Pig2Distrib}, from \code{\link[=pig2_distrib]{pig2_distrib()}}.}

\item{theta}{A named list with components \code{mu} (positive) and \code{alpha}
(positive), each a numeric vector of length 1 or \code{n}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of excess kurtoses, of length
\code{max(length(theta$mu), length(theta$alpha))}, positive throughout.
}
\description{
The cumulant ratio \eqn{\gamma_2 = \kappa_4/\kappa_2^{2}} of
\code{\link[=kurtosis.Pig1Distrib]{kurtosis.Pig1Distrib()}}, read at the dispersion
\eqn{\sigma = } \code{\link[=pig2_sigma]{pig2_sigma()}}\verb{(mu, alpha)}. As there, the ratio of the
fourth cumulant to the squared second is already the excess, so no 3 is
subtracted.
}
\section{Notation}{

\eqn{\mu > 0} is the mean, \eqn{\alpha > 0} the orthogonal dispersion and
\eqn{\kappa_k} the \eqn{k}-th cumulant.
}

\examples{
d <- pig2_distrib()

# The two parametrizations agree once the mapping is applied.
s <- distributions7:::pig2_sigma(3, 1)
all.equal(kurtosis(d, list(mu = 3, alpha = 1)),
          kurtosis(pig1_distrib(), list(mu = 3, sigma = s)))

}
\seealso{
\code{\link[=skewness.Pig2Distrib]{skewness.Pig2Distrib()}}, from the third cumulant;
\code{\link[=pig2_sigma]{pig2_sigma()}}; \code{\link[=kurtosis.Pig1Distrib]{kurtosis.Pig1Distrib()}}; \code{\link[=pig2_distrib]{pig2_distrib()}}.
}
\keyword{internal}
