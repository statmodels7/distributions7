% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mvstudent_t_distrib.R
\name{.mvt_scale_slices}
\alias{.mvt_scale_slices}
\title{Scale the Slices of an Array}
\usage{
.mvt_scale_slices(arr, v)
}
\arguments{
\item{arr}{A \eqn{p \times p \times n} numeric array.}

\item{v}{A numeric vector of length \eqn{n}. A shorter vector recycles
silently, as the arithmetic underneath would.}
}
\value{
A \eqn{p \times p \times n} numeric array with the same dimensions
as \code{arr}.
}
\description{
Multiplies the \eqn{i}th slice of a \eqn{p \times p \times n} array by
\code{v[i]}, which is how a term whose matrix part is the same at every
observation but whose scalar part is not enters an assembled derivative.
The multiplication is one vectorized recycle over the flattened array, so
nothing loops over the observations.
}
\examples{
arr <- array(1, c(2, 2, 3))
distributions7:::.mvt_scale_slices(arr, c(1, 10, 100))[1, 1, ]

# Slice by slice it is the scalar times the slice.
a <- array(rnorm(12), c(2, 2, 3))
s <- distributions7:::.mvt_scale_slices(a, c(2, -1, 0.5))
all.equal(s[, , 2], -a[, , 2])

}
\seealso{
\code{\link[=.mvt_const_slices]{.mvt_const_slices()}} for the case where the matrix is what stays
constant, \code{\link[=mv_outer_rows]{mv_outer_rows()}} for the array these are combined with, and
\code{\link[=distrib_cross2_y.MvStudentTDistrib]{distrib_cross2_y.MvStudentTDistrib()}} for the consumer.
}
\keyword{internal}
