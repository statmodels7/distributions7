% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skewnormal1_distrib.R
\name{distrib_expected_hessian.SkewNormal1Distrib}
\alias{distrib_expected_hessian.SkewNormal1Distrib}
\alias{distrib_dexpected_hessian.SkewNormal1Distrib}
\alias{distrib_d2expected_hessian.SkewNormal1Distrib}
\title{Skew Normal Expected Hessian and Its Derivatives}
\arguments{
\item{distrib}{A \code{SkewNormal1Distrib} object, from \code{\link[=skewnormal1_distrib]{skewnormal1_distrib()}}.}

\item{y}{A numeric vector of observations. Its length sets the length of
each returned component; the values themselves are not read.}

\item{theta}{A named list with components \code{mu}, \code{sigma} and \code{alpha}, each a
numeric vector of length 1 or of the length of \code{y}.}

\item{scale}{One of \code{"parameter"} (the default) or \code{"link"}, matched by
\code{\link[base:match.arg]{base::match.arg()}}.}

\item{approx, nsim}{Ignored.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}

\item{threads}{The thread count passed to the family's kernels.}
}
\value{
A named list of numeric vectors of length \code{length(y)}: for
\code{\link[=distrib_expected_hessian]{distrib_expected_hessian()}} the components keyed as \code{\link[=hess_names]{hess_names()}}, for
the two derivatives those keyed as \code{\link[=dexpected_names]{dexpected_names()}} and
\code{\link[=d2expected_names]{d2expected_names()}}.
}
\description{
Returns the expected information, and through \code{\link[=distrib_dexpected_hessian]{distrib_dexpected_hessian()}}
and \code{\link[=distrib_d2expected_hessian]{distrib_d2expected_hessian()}} its first and second derivatives in the
parameters.
}
\details{
No component has an elementary form. In Azzalini's notation, with
\eqn{b = \sqrt{2/\pi}} and
\eqn{a_k = \mathbb{E}\!\left[Z^k \{\phi(\alpha Z)/\Phi(\alpha Z)\}^2\right]}
for \eqn{Z} a standard skew normal of shape \eqn{\alpha},
\deqn{-\mathbb{E}[\ell_{\mu\mu}] = \frac{1 + \alpha^2 a_0}{\sigma^2},\quad
  -\mathbb{E}[\ell_{\sigma\sigma}] = \frac{2 + \alpha^2 a_2}{\sigma^2},\quad
  -\mathbb{E}[\ell_{\alpha\alpha}] = a_2,}
and each \eqn{a_k} is an integral that has no closed form. The family is a
location-scale family, so every component equals its value at
\eqn{\mu = 0}, \eqn{\sigma = 1} times \eqn{\sigma^{-k}}, with \eqn{k} the
number of indices on \eqn{\mu} or \eqn{\sigma}. The value there depends on
\eqn{\alpha} alone and is one integral over \eqn{z} of the analytic observed
derivatives against the density, taken once per distinct \eqn{\alpha} by the
exp-sinh rule of \code{\link[=loc_scale_expected]{loc_scale_expected()}}. It reproduces the three expressions
above, computed from the \eqn{a_k} by an independent quadrature, to the last
printed digit.

At \eqn{\alpha = 0} the information has rank 2 and not 3: its eigenvalues at
\eqn{\mu = 0}, \eqn{\sigma = 1} are 2, 1.637 and \eqn{-2.6\times10^{-27}},
and the smallest grows like \eqn{\alpha^4}, reading
\eqn{4.4\times10^{-10}} at \eqn{\alpha = 0.01} and \eqn{1.9\times10^{-3}} at
\eqn{\alpha = 0.5}. See \code{\link[=distrib_hessian.SkewNormal1Distrib]{distrib_hessian.SkewNormal1Distrib()}} for why.

\code{approx} and \code{nsim} are accepted for the generic's sake and ignored.
}
\examples{
d <- skewnormal1_distrib()
a <- 2
e <- distrib_expected_hessian(d, 0, list(mu = 0, sigma = 1, alpha = a))

# Azzalini's form, with the a_k computed by an independent quadrature.
ak <- sapply(c(0, 2), function(k) integrate(function(z)
  2 * z^k * exp(dnorm(z, log = TRUE) + 2 * dnorm(a * z, log = TRUE) -
                pnorm(a * z, log.p = TRUE)), -Inf, Inf, rel.tol = 1e-12)$value)
rbind(azzalini = c(1 + a^2 * ak[1], 2 + a^2 * ak[2], ak[2]),
      package = -c(e$mu_mu, e$sigma_sigma, e$alpha_alpha))
}
\seealso{
\code{\link[=loc_scale_expected]{loc_scale_expected()}} for the construction,
\code{\link[=distrib_hessian.SkewNormal1Distrib]{distrib_hessian.SkewNormal1Distrib()}} for the quantity this is the
expectation of, and \code{\link[=distrib_expected_hessian]{distrib_expected_hessian()}} for the generic.
}
