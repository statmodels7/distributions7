% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/negbin2_distrib.R
\name{distrib_pdf.NegBin2Distrib}
\alias{distrib_pdf.NegBin2Distrib}
\title{Negative Binomial Probability Mass Function, NB2}
\arguments{
\item{distrib}{A \code{NegBin2Distrib} object, from \code{\link[=negbin2_distrib]{negbin2_distrib()}}.}

\item{y}{A numeric vector of counts. A non-integer value gives 0 with a
warning from \code{\link[stats:NegBinomial]{stats::dnbinom()}}, and a negative value gives 0.}

\item{theta}{A named list with components \code{mu} and \code{theta}, each a numeric
vector of length 1 or of the length of \code{y}. A component of length 1 is
recycled. Both must be strictly positive.}

\item{log}{Logical of length 1. When \code{TRUE} the log-mass is returned.
Defaults to \code{FALSE}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of probabilities, of length
\code{max(length(y), length(mu), length(theta))}, one value per observation.
}
\description{
Computes the negative binomial mass
\deqn{P(Y = y; \mu, \theta) = \dfrac{\Gamma(y+\theta)}{y!\,\Gamma(\theta)}
      \left(\dfrac{\theta}{\theta+\mu}\right)^{\theta}
      \left(\dfrac{\mu}{\theta+\mu}\right)^{y}, \qquad
      y = 0, 1, 2, \ldots,}
by calling \code{\link[stats:NegBinomial]{stats::dnbinom()}} at \code{size = theta} and \code{mu = mu}, so the
accuracy is R's own. With \code{log = TRUE} the logarithm is formed inside
\code{dnbinom()} and stays finite far into the tail.

The mass is that of a Poisson whose rate is itself gamma distributed with
mean \eqn{\mu} and shape \eqn{\theta}, which is where the overdispersion
comes from. As \eqn{\theta} grows the gamma concentrates and the mass tends
to the Poisson's; at \eqn{\theta = 1} it is the geometric.
}
\examples{
d <- negbin2_distrib()
y <- c(0, 2, 6)
th <- list(mu = 4, theta = 2)

# The method is stats::dnbinom at size = theta, mu = mu.
all.equal(distrib_pdf(d, y, th), dnbinom(y, size = 2, mu = 4))

# At theta = 1 the family is the geometric.
all.equal(distrib_pdf(d, y, list(mu = 4, theta = 1)),
          distrib_pdf(geometric_distrib(), y, list(mu = 4)))

# At a large theta it is the Poisson, to six figures.
rbind(negbin = distrib_pdf(d, 0:4, list(mu = 3, theta = 1e6)),
      poisson = dpois(0:4, 3))

# The mass sums to one over the support.
sum(distrib_pdf(d, 0:400, th))
}
\seealso{
\code{\link[=distrib_cdf.NegBin2Distrib]{distrib_cdf.NegBin2Distrib()}} for the distribution function,
\code{\link[=distrib_gradient.NegBin2Distrib]{distrib_gradient.NegBin2Distrib()}} for the derivatives of the log-mass,
\code{\link[=distrib_pdf.NegBin1Distrib]{distrib_pdf.NegBin1Distrib()}} for the NB1 parametrization, and
\code{\link[=distrib_pdf]{distrib_pdf()}} for the generic.
}
