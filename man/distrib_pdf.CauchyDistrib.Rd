% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cauchy_distrib.R
\name{distrib_pdf.CauchyDistrib}
\alias{distrib_pdf.CauchyDistrib}
\title{Cauchy Probability Density Function}
\arguments{
\item{distrib}{A \code{CauchyDistrib} object, from \code{\link[=cauchy_distrib]{cauchy_distrib()}}.}

\item{y}{A numeric vector of observations. Every real value is in the
support, so no value is rejected.}

\item{theta}{A named list with components \code{mu} and \code{sigma}, each a numeric
vector of length 1 or of the length of \code{y}. A component of length 1 is
recycled. \code{sigma} must be strictly positive; a zero or negative value
gives \code{NaN} with a warning from \code{\link[stats:Cauchy]{stats::dcauchy()}}.}

\item{log}{Logical of length 1. When \code{TRUE} the log-density is returned.
Defaults to \code{FALSE}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of densities, of length
\code{max(length(y), length(mu), length(sigma))}, one value per observation.
}
\description{
Computes the Cauchy density
\deqn{f(y; \mu, \sigma) = \dfrac{1}{\pi \sigma \left[1 + \left(\dfrac{y-\mu}{\sigma}\right)^2\right]}}
by calling \code{\link[stats:Cauchy]{stats::dcauchy()}} at \code{location = mu} and \code{scale = sigma}. The
density decays like \eqn{y^{-2}}, so it stays representable far out where a
Gaussian of the same scale has underflowed to zero.
}
\examples{
d <- cauchy_distrib()
y <- c(-1.2, 0.3, 2.5)

# The method is stats::dcauchy at this parametrization.
all.equal(distrib_pdf(d, y, list(mu = 0.4, sigma = 1.5)),
          dcauchy(y, location = 0.4, scale = 1.5))

# A parameter may vary by observation, one value each.
distrib_pdf(d, y, list(mu = c(0, 1, 2), sigma = c(1, 1.5, 2)))

# Forty scale units out the Cauchy density is still an ordinary number,
# where the Gaussian of the same scale has underflowed.
distrib_pdf(d, 40, list(mu = 0, sigma = 1))
distrib_pdf(gaussian1_distrib(), 40, list(mu = 0, sigma = 1))
}
\seealso{
\code{\link[=distrib_cdf.CauchyDistrib]{distrib_cdf.CauchyDistrib()}} for the distribution function,
\code{\link[=distrib_gradient.CauchyDistrib]{distrib_gradient.CauchyDistrib()}} for the derivatives of the log-density,
\code{\link[=distrib_pdf.Gaussian1Distrib]{distrib_pdf.Gaussian1Distrib()}} for the light-tailed comparison, and
\code{\link[=distrib_pdf]{distrib_pdf()}} for the generic.
}
