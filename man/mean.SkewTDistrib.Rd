% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{mean.SkewTDistrib}
\alias{mean.SkewTDistrib}
\title{Mean of the Skew t Distribution}
\arguments{
\item{x}{A \code{SkewTDistrib}, from \code{\link[=skewt_distrib]{skewt_distrib()}}.}

\item{theta}{A named list with components \code{mu} (the location), \code{sigma} (the
scale, positive), \code{alpha} (the shape, any sign) and \code{nu} (the degrees of
freedom, positive), each a numeric vector of length 1 or \code{n}. Settings
with \eqn{\nu \le 1} give \code{NaN} in their own positions and do not affect
the others.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of means, of length equal to the longest of the
four components, \code{NaN} wherever \eqn{\nu \le 1}.
}
\description{
Closed form for \eqn{\nu > 1}: \eqn{E[Y] = \mu + \sigma\delta b_\nu}, with
\eqn{\delta = \alpha/\sqrt{1+\alpha^2}} and
\eqn{b_\nu = \sqrt{\nu/\pi}\,\Gamma\{(\nu-1)/2\}/\Gamma(\nu/2)}. At
\eqn{\nu \le 1} the value is \code{NaN}: the density is perfectly well defined
there and its first moment is not, so a number would be a claim the law does
not support.
}
\section{Notation}{

\eqn{\mu} is the location, \eqn{\sigma > 0} the scale, \eqn{\alpha} the
shape and \eqn{\nu > 0} the degrees of freedom, in Azzalini and Capitanio's
parametrization.
}

\examples{
d <- skewt_distrib()

# Symmetric at shape zero, so the location is the mean.
mean(d, list(mu = 0, sigma = 1, alpha = 0, nu = 5))

# The mean does not exist at or below one degree of freedom.
mean(d, list(mu = 0, sigma = 1, alpha = 2, nu = c(1, 5)))

}
\references{
Azzalini, A. and Capitanio, A. (2003). Distributions generated by
perturbation of symmetry with emphasis on a multivariate skew t
distribution. \emph{Journal of the Royal Statistical Society Series B} \strong{65},
367-389.
}
\seealso{
\code{\link[=variance.SkewTDistrib]{variance.SkewTDistrib()}}, whose threshold is 2;
\code{\link[=skewt_moment_pieces]{skewt_moment_pieces()}} for the shared quantities;
\code{\link[=mean.SkewNormal1Distrib]{mean.SkewNormal1Distrib()}}, the Gaussian-tailed limit; \code{\link[=skewt_distrib]{skewt_distrib()}}.
}
\keyword{internal}
