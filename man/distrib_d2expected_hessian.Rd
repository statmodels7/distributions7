% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dexpected_hessian.R
\name{distrib_d2expected_hessian}
\alias{distrib_d2expected_hessian}
\title{The Second Derivative of the Expected Information}
\usage{
distrib_d2expected_hessian(
  distrib,
  y,
  theta,
  scale = c("parameter", "link"),
  approx = c("opg", "bartlett", "integrate", "mc"),
  nsim = 10000,
  ...
)
}
\arguments{
\item{distrib}{A distribution object inheriting from \code{distrib}.}

\item{y}{A numeric vector of observations.}

\item{theta}{A named list of parameters, each of length 1 or
\code{length(y)}.}

\item{scale}{\code{"parameter"} or \code{"link"}.}

\item{approx, nsim}{Accepted for symmetry with
\code{\link[=distrib_dexpected_hessian]{distrib_dexpected_hessian()}}; no method reads them.}

\item{...}{Passed to methods.}
}
\value{
A named list of numeric vectors, keyed as
\code{\link[=d2expected_names]{d2expected_names(distrib@params)}}.
}
\description{
\eqn{\partial^2\,\mathbb{E}[\ell_{ab}]/\partial\theta_c\,\partial\theta_d},
one component per pair \eqn{(a,b)} and per pair \eqn{(c,d)}.
}
\details{
Differentiating the identity of \code{\link[=distrib_dexpected_hessian]{distrib_dexpected_hessian()}} once more
moves the measure a second time,
\deqn{\frac{\partial^2}{\partial\theta_c\,\partial\theta_d}\mathbb{E}[\ell_{ab}]
  = \mathbb{E}[\ell_{abcd}] + \mathbb{E}[\ell_{abd}\ell_{c}]
  + \mathbb{E}[\ell_{abc}\ell_{d}] + \mathbb{E}[\ell_{ab}\ell_{cd}]
  + \mathbb{E}[\ell_{ab}\ell_{c}\ell_{d}],}
and no Bartlett identity isolates those moments. A family supplies the
components as ordinary derivatives of its own written-out expected
information; the components are symmetric in \eqn{(a,b)} and in \eqn{(c,d)}
separately, and are keyed by \code{\link[=d2expected_names]{d2expected_names()}}.

\strong{There is no numerical default.} A difference of
\code{\link[=distrib_dexpected_hessian]{distrib_dexpected_hessian()}}, whose own default is already a difference,
would be the nested differencing the package forbids, so the base method
signals an error and a family that does not register one has no second
derivative. The families that do are \code{gaussian1_distrib()},
\code{poisson_distrib()}, \code{gamma1_distrib()}, \code{negbin2_distrib()},
\code{beta1_distrib()} and the seventeen listed on
\link{distrib_dexpected_hessian.elementary}, each from a compiled kernel.

On \code{scale = "link"} the expected information is
\eqn{F_{ab} = \mathbb{E}[\ell_{ab}]\,h_a' h_b'}, with no term in \eqn{h''}
because \eqn{\mathbb{E}[\ell_a] = 0}, and its derivatives follow by
Leibniz's rule, written once in \code{\link[=dexpected_link]{dexpected_link()}}.
}
\examples{
d <- gaussian1_distrib()
str(distrib_d2expected_hessian(d, 0, list(mu = 0, sigma = 1)))

}
\seealso{
\code{\link[=distrib_dexpected_hessian]{distrib_dexpected_hessian()}}, \code{\link[=d2expected_names]{d2expected_names()}}
}
