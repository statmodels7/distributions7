% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/invgauss1_distrib.R
\name{distrib_deriv4.InvGauss1Distrib}
\alias{distrib_deriv4.InvGauss1Distrib}
\title{Inverse Gaussian Fourth-Order Derivatives in Mean and Dispersion}
\arguments{
\item{distrib}{An \code{InvGauss1Distrib} object, from \code{\link[=invgauss1_distrib]{invgauss1_distrib()}}.}

\item{y}{A numeric vector of strictly positive observations. With
\code{expected = TRUE} only its length is used.}

\item{theta}{A named list with components \code{mu} and \code{phi}, each a numeric
vector of length 1 or of the length of \code{y}. A component of length 1 is
recycled. Both must be strictly positive.}

\item{expected}{Logical of length 1. When \code{TRUE} the expectations under the
model are returned in place of the observed values. Defaults to \code{FALSE}.}

\item{scale}{One of \code{"parameter"} (the default) or \code{"link"}, matched by
\code{\link[base:match.arg]{base::match.arg()}}. Read by the generic, not by this method.}

\item{approx}{Ignored, a closed form being available for both the observed
and the expected values.}

\item{nsim}{Ignored, for the same reason. Defaults to \code{10000}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}

\item{threads}{A single positive integer, how many threads the kernel may
use. Below the measured internal threshold the kernel stays sequential
whatever the count says. Defaults to \code{1L}.}
}
\value{
A named list of five numeric vectors, \code{mu_mu_mu_mu},
\code{mu_mu_mu_phi}, \code{mu_mu_phi_phi}, \code{mu_phi_phi_phi} and \code{phi_phi_phi_phi},
each of length \code{max(length(y), length(mu), length(phi))}.
}
\description{
Computes the five distinct fourth derivatives of the inverse Gaussian
log-density with respect to \eqn{\mu} and \eqn{\phi}, in closed form, by the
same route the lower orders take: the log-density is a linear combination of
\eqn{y}, \eqn{1/y} and \eqn{\log y} with coefficients rational in the
parameters.

With \code{expected = TRUE} the expectations are returned, obtained by replacing
\eqn{Y} with \eqn{\mu} and \eqn{1/Y} with \eqn{1/\mu + \phi}. Both routes
are closed form, so \code{approx} and \code{nsim} are ignored.
}
\section{Notation}{

\eqn{\ell^{(ijkl)}} is the fourth derivative of the log-density in
parameters \eqn{i}, \eqn{j}, \eqn{k} and \eqn{l}. Parenthesized
superscripts name derivatives.
}

\examples{
d <- invgauss1_distrib()
y <- c(0.5, 1, 2)
th <- list(mu = 1, phi = 2)
d4 <- distrib_deriv4(d, y, th)
names(d4)

# The expected values are constants at a fixed parameter setting.
lapply(distrib_deriv4(d, y, th, expected = TRUE), unique)

# A central difference of the third order reproduces it.
eps <- 1e-6
up <- distrib_deriv3(d, y, list(mu = 1 + eps, phi = 2))$mu_mu_mu
dn <- distrib_deriv3(d, y, list(mu = 1 - eps, phi = 2))$mu_mu_mu
all.equal((up - dn) / (2 * eps), d4$mu_mu_mu_mu, tolerance = 1e-3)
}
\seealso{
\code{\link[=distrib_deriv3.InvGauss1Distrib]{distrib_deriv3.InvGauss1Distrib()}} for the order below,
\code{\link[=distrib_deriv4]{distrib_deriv4()}} for the generic, and
\code{\link[=distrib_expected_hessian.InvGauss1Distrib]{distrib_expected_hessian.InvGauss1Distrib()}} for the second-order
expectation these extend.
}
