% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/numerical_functions.R
\name{numerical_series}
\alias{numerical_series}
\title{Numerical Summation of Discrete Series (Rcpp Accelerated)}
\usage{
numerical_series(
  f,
  start = 0,
  end = Inf,
  step = 1000,
  tol = 1e-10,
  maxit = 1000000L,
  reltol = TRUE
)
}
\arguments{
\item{f}{A function taking a vector of integers \code{x} and returning a vector of numeric values.
\strong{Must be vectorized}.}

\item{start}{Numeric. Starting value. Can be finite, \code{Inf}, or \code{-Inf}. Defaults to \code{0}.}

\item{end}{Numeric. Ending value. Can be finite, \code{Inf}, or \code{-Inf}. Defaults to \code{Inf}.}

\item{step}{Integer. Number of terms to calculate in a single vectorized batch. Defaults to \code{1000}.}

\item{tol}{Numeric. Tolerance threshold for convergence. Defaults to \code{1e-10}.}

\item{maxit}{Integer. Safety limit for the maximum number of batch iterations. Defaults to \code{1000000}.}

\item{reltol}{Logical. If \code{TRUE} (default), uses a hybrid relative tolerance.}
}
\value{
A numeric scalar representing the calculated sum.
}
\description{
Calculates the sum of a function \code{f(x)} over a sequence of integers from \code{start} to \code{end}.
The function is designed to handle finite sums, one-sided infinite series, and
doubly infinite series by automatically adapting its summation strategy.
}
\details{
\strong{1. Summation Strategies:}
The function automatically detects the domain topology based on \code{start} and \code{end}:
\itemize{
\item \strong{Forward (Standard):} If \code{start <= end} (e.g., \code{1} to \code{Inf}).
\item \strong{Backward (Reflection):} If \code{start > end} (e.g., \code{-1} to \code{-Inf}), evaluates \code{f(-x)}.
\item \strong{Doubly Infinite (Folding):} If \code{start == -Inf} and \code{end == Inf}, folds around 0.
}

\strong{2. Speed and Convergence:}
Powered by Rcpp. The summation is performed in vectorized blocks of size \code{step}.
It monitors convergence using the sum of absolute values in the current chunk, preventing premature stops on alternating series while maintaining high precision.

\strong{3. Underflow & Divergence Detection:}
Includes heuristics to stop early if the sequence starts growing in absolute terms
(divergence), or skips up to 50 empty chunks to protect from premature stopping
when \code{f(x)} evaluates exactly to \code{0} at the start.
}
