% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdf_derivatives_families.R
\name{distrib_hess_cdf.SkewTDistrib}
\alias{distrib_hess_cdf.SkewTDistrib}
\title{Skew t Log-CDF Hessian}
\arguments{
\item{distrib}{A \code{SkewTDistrib} object, from \code{\link[=skewt_distrib]{skewt_distrib()}}.}

\item{q}{A numeric vector of quantiles.}

\item{theta}{A named list with components \code{mu}, \code{sigma} (positive), \code{alpha}
(any sign) and \code{nu} (positive), each a numeric vector of length 1 or \code{n}.}

\item{lower.tail}{Is the lower tail wanted? A single logical, \code{TRUE} by
default.}

\item{log}{Are derivatives of the log probability wanted? A single logical,
\code{TRUE} by default.}
}
\value{
A named list of ten numeric vectors keyed as \code{\link[=hess_names]{hess_names()}}, each
the length of \code{q} recycled against \code{theta}.
}
\description{
Closed form in the location-scale block, three of the ten components; the
seven touching the shape or the degrees of freedom are differenced. The
method is \code{\link[=partial_loc_scale_hess_cdf]{partial_loc_scale_hess_cdf()}} itself, shared with the Student t
and the pseudo-Huber.
}
\section{Notation}{

\eqn{\mu} is the location, \eqn{\sigma > 0} the scale, \eqn{\alpha} the
shape, \eqn{\nu > 0} the degrees of freedom, \eqn{z = (q-\mu)/\sigma} and
\eqn{f} the density.
}

\examples{
d <- skewt_distrib()
th <- list(mu = 0.3, sigma = 1.2, alpha = 2, nu = 6)

# Ten components, of which mu_mu, sigma_sigma and mu_sigma are closed.
names(distrib_hess_cdf(d, c(-1, 2), th))

}
\seealso{
\code{\link[=partial_loc_scale_hess_cdf]{partial_loc_scale_hess_cdf()}} for the shared body;
\code{\link[=distrib_grad_cdf.SkewTDistrib]{distrib_grad_cdf.SkewTDistrib()}} for the first order;
\code{\link[=skewt_distrib]{skewt_distrib()}}.
}
\keyword{internal}
