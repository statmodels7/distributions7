% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vonmises1_distrib.R
\name{distrib_cdf.VonMises1Distrib}
\alias{distrib_cdf.VonMises1Distrib}
\title{von Mises Cumulative Distribution Function}
\arguments{
\item{distrib}{A \code{VonMises1Distrib} object, from \code{\link[=vonmises1_distrib]{vonmises1_distrib()}}.}

\item{q}{A numeric vector of angles.}

\item{theta}{A named list with components \code{mu} and \code{kappa}, each a numeric
vector of length 1 or of the length of \code{q}. A component of length 1 is
recycled. \code{mu} must lie in \eqn{(-\pi, \pi)} and \code{kappa} be strictly
positive; a non-positive \code{kappa} gives \code{NA}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.
This method takes \strong{no} \code{lower.tail} or \code{log.p}: the upper tail is
\code{1 - F(q)} and the logarithm is \code{log(F(q))}.}
}
\value{
A numeric vector of probabilities in \eqn{[0, 1]}, of length
\code{max(length(q), length(mu), length(kappa))}.
}
\description{
Computes \eqn{F(q) = P(Y \le q)} on \eqn{[-\pi, \pi)} from the Fourier
series of the density integrated term by term, through \code{\link[=vm_cdf]{vm_cdf()}}. The
density has no elementary antiderivative, and the quadrature the base class
would use costs one integration per observation; the series replaces
\eqn{n} quadratures with a few dozen vectorized steps, using only the Bessel
\strong{ratios} \eqn{I_j/I_0} from a backward recurrence.

Below \eqn{-\pi} the value is 0 and at or above \eqn{\pi} it is 1, the
support being the declared interval. The result is clamped to
\eqn{[0, 1]}: the series is exact and its rounding is not.
}
\examples{
d <- vonmises1_distrib()
th <- list(mu = 0.5, kappa = 2)
y <- c(-1, 0, 0.5, 2)

# The series agrees with a direct quadrature of the density.
rbind(series = distrib_cdf(d, y, th),
      quadrature = vapply(y, function(v)
        integrate(function(u) distrib_pdf(d, u, th), -pi, v)$value,
        numeric(1)))

# It runs from 0 to 1 across the declared support.
c(distrib_cdf(d, -pi, th), distrib_cdf(d, pi - 1e-12, th))

# And the quantile function, which the base class obtains by root finding
# on this, inverts it.
q <- distrib_quantile(d, c(0.25, 0.5, 0.75), th)
all.equal(distrib_cdf(d, q, th), c(0.25, 0.5, 0.75), tolerance = 1e-6)
}
\seealso{
\code{\link[=vm_cdf]{vm_cdf()}} for the series and its measured term count,
\code{\link[=distrib_pdf.VonMises1Distrib]{distrib_pdf.VonMises1Distrib()}} for the density,
\code{\link[=distrib_quantile]{distrib_quantile()}}, which inverts this by root finding, and
\code{\link[=distrib_cdf]{distrib_cdf()}} for the generic.
}
