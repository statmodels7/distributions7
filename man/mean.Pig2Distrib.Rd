% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{mean.Pig2Distrib}
\alias{mean.Pig2Distrib}
\title{Mean of the Orthogonal Poisson-Inverse Gaussian Distribution}
\arguments{
\item{x}{A \code{Pig2Distrib}, from \code{\link[=pig2_distrib]{pig2_distrib()}}.}

\item{theta}{A named list with components \code{mu} (the mean, positive) and
\code{alpha} (the orthogonal dispersion, positive), each a numeric vector of
length 1 or \code{n}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of means, of length
\code{max(length(theta$mu), length(theta$alpha))}. The value is the mean
parameter itself, the dispersion entering neither it nor the length.
}
\description{
Closed form: \eqn{E[Y] = \mu}. This is the same law as \code{\link[=pig1_distrib]{pig1_distrib()}}
written by a dispersion \eqn{\alpha} chosen so that the mean and the
dispersion are orthogonal in the expected information; the mapping is
\code{\link[=pig2_sigma]{pig2_sigma()}}, and the mean is the first parameter either way.
}
\section{Notation}{

\eqn{\mu > 0} is the mean and \eqn{\alpha > 0} the orthogonal dispersion.
}

\examples{
d <- pig2_distrib()

# The first parameter is the mean, and the dispersion does not move it.
mean(d, list(mu = c(1, 2, 3), alpha = 1))

}
\seealso{
\code{\link[=variance.Pig2Distrib]{variance.Pig2Distrib()}}, where the dispersion does enter;
\code{\link[=pig2_sigma]{pig2_sigma()}} for the mapping; \code{\link[=mean.Pig1Distrib]{mean.Pig1Distrib()}};
\code{\link[=pig2_distrib]{pig2_distrib()}}.
}
\keyword{internal}
