% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skewt_distrib.R
\name{skewt_msa_derivs}
\alias{skewt_msa_derivs}
\title{Every Skew t Derivative of an Order That Avoids the Degrees of Freedom}
\usage{
skewt_msa_derivs(distrib, y, theta, order)
}
\arguments{
\item{distrib}{A \code{SkewTDistrib} object, from \code{\link[=skewt_distrib]{skewt_distrib()}}.}

\item{y}{A numeric vector of observations.}

\item{theta}{A named list with components \code{mu}, \code{sigma}, \code{alpha} and \code{nu}.}

\item{order}{A single integer, the order of differentiation.}
}
\value{
A named list of numeric vectors, a subset of \code{\link[=deriv_names]{deriv_names()}} at that
order, holding every component free of \eqn{\nu}.
}
\description{
Returns, in closed form, the components of a given order whose indices are
all drawn from \eqn{(\mu, \sigma, \alpha)}: ten of the twenty at order
three and fifteen of the thirty-five at order four.
}
\details{
The multi-indices and the names come from \code{\link[=deriv_indices]{deriv_indices()}} and
\code{\link[=deriv_names]{deriv_names()}}, the same enumeration, so a name is never recovered by
splitting a string.
}
\examples{
d <- skewt_distrib()
m <- distributions7:::skewt_msa_derivs(
  d, c(-0.4, 1.2), list(mu = 0, sigma = 1, alpha = 0.7, nu = 8), 3L)
names(m)

}
\seealso{
\code{\link[=skewt_msa_tower]{skewt_msa_tower()}} for the derivation and
\code{\link[=distrib_deriv3.SkewTDistrib]{distrib_deriv3.SkewTDistrib()}} for the method that reads this.
}
\keyword{internal}
