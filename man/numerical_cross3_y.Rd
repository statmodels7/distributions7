% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross3_derivatives.R
\name{numerical_cross3_y}
\alias{numerical_cross3_y}
\title{Numerical Mixed Third-Response Parameter Derivatives}
\usage{
numerical_cross3_y(
  distrib,
  y,
  theta,
  h_rel = .Machine$double.eps^(1/3),
  which = NULL
)
}
\arguments{
\item{distrib}{A distribution object inheriting from \code{continuous_distrib}.}

\item{y}{A numeric vector of observations.}

\item{theta}{A named list of parameters, the point to differentiate at.}

\item{h_rel}{The relative step, defaulting to \code{.Machine$double.eps^(1/3)},
the optimal exponent for a central first difference.}

\item{which}{An optional character vector naming a subset of
\code{distrib@params}. The default, \code{NULL}, computes every component.}
}
\value{
A named list with one numeric vector per requested parameter, each
as long as \code{y}.
}
\description{
Computes \eqn{\partial^4 \ell / \partial y^3\, \partial \theta_i} by one
central difference of \code{\link[=distrib_deriv3_y]{distrib_deriv3_y()}} in each parameter. It is what the
default \code{\link[=distrib_cross3_y]{distrib_cross3_y()}} method runs for a continuous family that is not
a location family.
}
\details{
A family carrying an analytic \code{distrib_deriv3_y} pays for exactly one
difference. Where that derivative is itself the base class's stencil on the
log-density, the two differences act on different variables and compose
into one mixed stencil. The step is chosen by \code{\link[=fd_stable_step]{fd_stable_step()}}, so a
parameter near a bound is differenced on the side the bound allows.
}
\examples{
d <- student_t1_distrib()
y <- c(-1, 0, 2)
theta <- list(mu = 0.4, sigma = 1.3, nu = 6)

# Against the identity the location family uses.
max(abs(unlist(numerical_cross3_y(d, y, theta)) -
        unlist(distrib_cross3_y(d, y, theta))))

numerical_cross3_y(d, y, theta, which = "nu")

}
\seealso{
\code{\link[=distrib_cross3_y]{distrib_cross3_y()}}, the generic it serves, and
\code{\link[=distrib_deriv3_y]{distrib_deriv3_y()}} for the quantity it differences.
}
