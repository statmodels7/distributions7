% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometric_distrib.R
\name{distrib_deriv4.GeometricDistrib}
\alias{distrib_deriv4.GeometricDistrib}
\title{Geometric Fourth-Order Derivative}
\arguments{
\item{distrib}{A \code{GeometricDistrib} object, from \code{\link[=geometric_distrib]{geometric_distrib()}}.}

\item{y}{A numeric vector of counts. With \code{expected = TRUE} only its length
is used.}

\item{theta}{A named list with the single component \code{mu}, a numeric vector
of length 1 or of the length of \code{y}. \code{mu} must be strictly positive.}

\item{expected}{Logical of length 1. When \code{TRUE} the expectation under the
model is returned in place of the observed value. Defaults to \code{FALSE}.}

\item{scale}{One of \code{"parameter"} (the default) or \code{"link"}, matched by
\code{\link[base:match.arg]{base::match.arg()}}. Read by the generic, not by this method.}

\item{approx}{Ignored, a closed form being available for both routes.}

\item{nsim}{Ignored, for the same reason. Defaults to \code{10000}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}

\item{threads}{A single positive integer, how many threads the kernel may
use. Defaults to \code{1L}.}
}
\value{
A named list of one numeric vector, \code{mu_mu_mu_mu}, of length
\code{max(length(y), length(mu))}.
}
\description{
Computes the fourth derivative of the geometric log-mass with respect to the
mean, in closed form. As at third order it is a combination of
\eqn{\mu^{-k}} and \eqn{(1+\mu)^{-k}} with the data entering linearly
through \eqn{y}.

With \code{expected = TRUE} the expectation is returned, obtained by substituting
\eqn{\mathbb{E}[Y] = \mu}. Both routes are closed form, so \code{approx} and
\code{nsim} are ignored.
}
\section{Notation}{

\eqn{\ell^{(\mu\mu\mu\mu)}} is the fourth derivative of the log-mass with
respect to \eqn{\mu}. Parenthesized superscripts name derivatives.
}

\examples{
d <- geometric_distrib()
y <- c(0, 2, 7)
th <- list(mu = 3)

# A central difference of the third order reproduces the fourth.
eps <- 1e-6
up <- distrib_deriv3(d, y, list(mu = 3 + eps))$mu_mu_mu
dn <- distrib_deriv3(d, y, list(mu = 3 - eps))$mu_mu_mu
all.equal((up - dn) / (2 * eps), distrib_deriv4(d, y, th)$mu_mu_mu_mu,
          tolerance = 1e-5)

# The expected value is one number.
unique(distrib_deriv4(d, y, th, expected = TRUE)$mu_mu_mu_mu)
}
\seealso{
\code{\link[=distrib_deriv3.GeometricDistrib]{distrib_deriv3.GeometricDistrib()}} for the order below;
\code{\link[=distrib_deriv4]{distrib_deriv4()}} for the generic.
}
