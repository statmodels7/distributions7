% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kink_decomposition.R
\name{params_order}
\alias{params_order}
\title{The Order of Differentiability of a Family in Each Parameter}
\usage{
params_order(distrib, ...)
}
\arguments{
\item{distrib}{An object inheriting from class \code{"distrib"}.}

\item{...}{Passed to \code{\link[=kink_decomposition]{kink_decomposition()}}.}
}
\value{
A named numeric vector, one entry per element of \code{distrib@params}.
}
\description{
One integer per parameter: the largest \code{k} such that the log-density has \code{k}
continuous derivatives in that parameter everywhere in \code{y}. \code{Inf} says the
parameter is smooth, which is the answer for every family that declares no
\code{\link[=kink_decomposition]{kink_decomposition()}}.
}
\details{
The order is DEDUCED and not declared. Where a family declares a
decomposition \eqn{c(\theta)\phi(v(y,\theta))}, a parameter the kink moves
with -- one whose \eqn{\partial v/\partial \theta_p} is not identically zero
-- inherits the order of \eqn{\phi}, and every other parameter stays \code{Inf}.
Whether \eqn{\partial v/\partial \theta_p} vanishes is measured at probe
values rather than assumed, so a Huber cut-off held at zero reports the
scale as smooth, which it then is.

A family that declares no decomposition but records a parameter as
non-smooth through \code{params_smooth} reports \code{NA} for it: there is a kink and
its order has not been established. A consumer must treat that as unusable
rather than as smooth, which is what \code{\link[=check_distrib]{check_distrib()}} and the fitting layer
do. This is the state of every wrapper of a kinked family today.

The consumers and the order each one needs:

\tabular{ll}{
1 \tab a score continuous in the coefficients \cr
2 \tab IWLS, Newton, \code{vcov()}, and the determinant of a Laplace criterion \cr
3 \tab the exact gradient of a marginal criterion \cr
4 \tab its exact Hessian \cr
5 \tab the fourth derivative of a filtered predictor
}
}
\examples{
# smooth everywhere
params_order(gaussian1_distrib())

# the Laplace: the location carries the kink, the scale does not
params_order(laplace_distrib())

}
\seealso{
\code{\link[=kink_decomposition]{kink_decomposition()}}, \code{\link[=kink_spec]{kink_spec()}}, \code{\link[=param_smoothness]{param_smoothness()}}.
}
