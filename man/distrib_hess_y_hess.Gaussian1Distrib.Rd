% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_theta2_derivatives.R
\name{distrib_hess_y_hess.Gaussian1Distrib}
\alias{distrib_hess_y_hess.Gaussian1Distrib}
\title{Gaussian Hyperparameter Hessian of the Response Curvature}
\arguments{
\item{distrib}{A \code{Gaussian1Distrib} object.}

\item{y}{A numeric vector of observations. Only its length is used.}

\item{theta}{A named list with \code{mu} and \code{sigma}.}

\item{scale}{One of \code{"parameter"} or \code{"link"}, applied by the generic before
dispatch.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A named list of three numeric vectors of length \code{length(y)}, keyed
\code{mu_mu}, \code{sigma_sigma} and \code{mu_sigma}, of which the first and last are
exactly zero.
}
\description{
Closed form, and nearly all of it zero. The gaussian's response curvature is
\eqn{-1/\sigma^2}, which carries no location at all, so
\deqn{\frac{\partial^4\ell}{\partial y^2\partial\sigma^2}
    = -\frac{6}{\sigma^4}}
is the only component that does not vanish, and it does not vary with the
data either.
}
\section{Notation}{

\eqn{\ell} is the log-density of one observation, \eqn{y} the response,
\eqn{\theta_i} a distribution parameter, \eqn{\eta_i} its value on the
unconstrained scale and \eqn{h_i = g_i^{-1}} the inverse link carrying one to
the other.
}

\examples{
d <- gaussian1_distrib()
y <- c(-1, 0, 2)
theta <- list(mu = 0.4, sigma = 1.3)
h <- distrib_hess_y_hess(d, y, theta)
h

# One surviving component, constant along y.
c(reported = h$sigma_sigma[1], formula = -6 / 1.3^4)

# Against a numerical Hessian of the response curvature.
f <- function(v) distrib_hess_y(d, y[1], list(mu = v[1], sigma = v[2]))
numDeriv::hessian(f, c(0.4, 1.3))
}
\seealso{
\code{\link[=distrib_hess_y_hess]{distrib_hess_y_hess()}} for the generic,
\code{\link[=distrib_grad_y_hess.Gaussian1Distrib]{distrib_grad_y_hess.Gaussian1Distrib()}} for the third-order twin, and
\code{\link[=distrib_cross2_y]{distrib_cross2_y()}} for the first-order column.
}
\keyword{internal}
