% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dexpected_reparam.R
\name{reparam_dexpected}
\alias{reparam_dexpected}
\title{The Expected Information's Derivatives Through a Reparametrization}
\usage{
reparam_dexpected(distrib, y, theta, order, threads = 1L)
}
\arguments{
\item{distrib}{A reparametrized distribution.}

\item{y}{The response.}

\item{theta}{A named list of the new parameters.}

\item{order}{\code{1L} or \code{2L}.}

\item{threads}{The thread count passed to the parent.}
}
\value{
A named list on the parameter scale, keyed as \code{\link[=dexpected_names]{dexpected_names()}}
or \code{\link[=d2expected_names]{d2expected_names()}}.
}
\description{
Carries the parent's \eqn{\partial E/\partial\theta} and
\eqn{\partial^2 E/\partial\theta^2} into the new coordinates \eqn{\phi}.
}
\details{
The expected information transforms as a tensor, with no term in the map's
second derivative because the score has mean zero:
\eqn{\tilde E_{ab} = \sum_{ij} E_{ij} J_{ia} J_{jb}}, with
\eqn{J_{ia} = \partial\theta_i/\partial\phi_a}. Writing \eqn{J_{ia,c}} and
\eqn{J_{ia,cd}} for the map's second and third derivatives,
\deqn{\partial_c\tilde E_{ab} = \sum_{ij}\Big[\sum_k \partial_k E_{ij}
  J_{kc}\Big] J_{ia}J_{jb} + \sum_{ij} E_{ij}(J_{ia,c}J_{jb} + J_{ia}J_{jb,c}),}
and \eqn{\partial_{cd}\tilde E_{ab}} follows by Leibniz's rule once more,
reading the parent's second derivative and the map's third. The map's
partials are the keyed tables of \code{\link[=reparam_tables]{reparam_tables()}}, hand-written for the
shipped reparametrizations, so every term is analytic wherever the
parent's derivatives are.
}
\seealso{
\code{\link[=reparam_chain]{reparam_chain()}}, \code{\link[=dexpected_link]{dexpected_link()}}
}
\keyword{internal}
