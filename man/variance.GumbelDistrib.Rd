% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{variance.GumbelDistrib}
\alias{variance.GumbelDistrib}
\title{Variance of the Gumbel Distribution}
\arguments{
\item{x}{A \code{GumbelDistrib}, from \code{\link[=gumbel_distrib]{gumbel_distrib()}}.}

\item{theta}{A named list with components \code{mu} (the location) and \code{sigma}
(the scale, positive), each a numeric vector of length 1 or \code{n}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of variances, of length
\code{max(length(theta$mu), length(theta$sigma))}. The location does not
enter the value, so a setting that varies \code{mu} alone repeats one number.
}
\description{
Closed form: \eqn{\operatorname{Var}(Y) = \pi^2\sigma^2/6}. The scale is not
the standard deviation: it is smaller by \eqn{\pi/\sqrt6 \approx 1.2825}.
The location does not enter, the family being location-scale.
}
\examples{
d <- gumbel_distrib()

# pi^2 sigma^2 / 6.
all.equal(variance(d, list(mu = 0, sigma = 2)), pi^2 * 4 / 6)

# The standard deviation is pi / sqrt(6) times the scale.
std_dev(d, list(mu = 0, sigma = 1))

}
\seealso{
\code{\link[=mean.GumbelDistrib]{mean.GumbelDistrib()}}, \code{\link[=kurtosis.GumbelDistrib]{kurtosis.GumbelDistrib()}},
\code{\link[=gumbel_distrib]{gumbel_distrib()}}.
}
\keyword{internal}
