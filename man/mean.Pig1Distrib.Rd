% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{mean.Pig1Distrib}
\alias{mean.Pig1Distrib}
\title{Mean of the Poisson-Inverse Gaussian Distribution}
\arguments{
\item{x}{A \code{Pig1Distrib}, from \code{\link[=pig1_distrib]{pig1_distrib()}}.}

\item{theta}{A named list with components \code{mu} (the mean, positive) and
\code{sigma} (the dispersion, positive), each a numeric vector of length 1 or
\code{n}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of means, the length of \code{theta$mu}. The dispersion
does not enter the value and does not lengthen it.
}
\description{
Closed form: \eqn{E[Y] = \mu}, the first cumulant. The family is a Poisson
whose rate is mixed over an inverse Gaussian of mean 1, so the mixing leaves
the mean where the Poisson put it and shows only from the second cumulant
onwards.
}
\section{Notation}{

\eqn{\mu > 0} is the mean and \eqn{\sigma > 0} the dispersion of the mixing
inverse Gaussian.
}

\examples{
d <- pig1_distrib()

# The first parameter is the mean, and the dispersion does not move it.
mean(d, list(mu = c(1, 2, 3), sigma = 1))

}
\seealso{
\code{\link[=variance.Pig1Distrib]{variance.Pig1Distrib()}}, which exceeds this;
\code{\link[=mean.Pig2Distrib]{mean.Pig2Distrib()}} for the orthogonal parametrization;
\code{\link[=mean.PoissonDistrib]{mean.PoissonDistrib()}}, the \eqn{\sigma \to 0} limit; \code{\link[=pig1_distrib]{pig1_distrib()}}.
}
\keyword{internal}
