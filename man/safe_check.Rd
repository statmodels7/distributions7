% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_distrib.R
\name{safe_check}
\alias{safe_check}
\title{Run a Check, Turning an Error Into a Failure}
\usage{
safe_check(name, expr)
}
\arguments{
\item{name}{The check's name, used for the row built on failure.}

\item{expr}{The expression to evaluate; normally returns a row from
\code{\link[=new_check]{new_check()}}.}
}
\value{
The value of \code{expr}, or a failed row carrying the error message.
}
\description{
Evaluates a check expression and converts any error into a failed row rather
than letting it abort the report.
}
\details{
A distribution under validation is by assumption possibly broken, so a check
that throws is itself a result. Without this, the first component to raise
would end the run and hide every check after it, which is the least useful
moment to
stop being informative.
}
\seealso{
\code{\link[=check_distrib]{check_distrib()}}, \code{\link[=new_check]{new_check()}}
}
\keyword{internal}
