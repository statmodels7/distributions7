% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expected_loc_scale.R
\name{expected_hessian_by_quadrature}
\alias{expected_hessian_by_quadrature}
\title{Is a Family's Expected Information Computed by Quadrature?}
\usage{
expected_hessian_by_quadrature(x, ...)
}
\arguments{
\item{x}{An object inheriting from class \code{"distrib"}.}

\item{...}{Passed to methods.}
}
\value{
A single logical.
}
\description{
\code{TRUE} when the family's expected information is exact but obtained by a
quadrature over the standardized response for each distinct value of its
shape parameters, as \code{\link[=loc_scale_expected]{loc_scale_expected()}} does; \code{FALSE} otherwise.
}
\details{
The answer is a statement about cost, and it is read beside
\code{\link[=expected_hessian_exact]{expected_hessian_exact()}}, which states accuracy. A family answering \code{TRUE}
to both computes its expected information exactly, at one integral per
distinct shape: nothing where the shape is the same for every observation,
and one integral per observation where it is modelled. Measured on smooth
regressions at 1000 observations with the shape developed over a covariate,
a fit that takes the scoring step on this information costs 7 to 21 times
one that takes it on the observed information, at the same estimate.
\pkg{statmodels7}'s \code{iwls(hessian = "auto")} therefore settles on the
observed information for such a family.

Four shipped families answer \code{TRUE}: \code{\link[=skewnormal1_distrib]{skewnormal1_distrib()}},
\code{\link[=skewnormal2_distrib]{skewnormal2_distrib()}}, \code{\link[=skewt_distrib]{skewt_distrib()}} and \code{\link[=pseudohuber_distrib]{pseudohuber_distrib()}}. The
default answers for a wrapper's parent, read off its \code{parent_distrib}
property, and \code{FALSE} for a family without one.
}
\examples{
expected_hessian_by_quadrature(skewt_distrib())
expected_hessian_by_quadrature(gaussian1_distrib())
expected_hessian_by_quadrature(fixed(skewt_distrib(), nu = 6))

}
\seealso{
\code{\link[=expected_hessian_exact]{expected_hessian_exact()}}, \code{\link[=loc_scale_expected]{loc_scale_expected()}}
}
