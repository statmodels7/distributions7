% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logistic_distrib.R
\name{distrib_quantile.LogisticDistrib}
\alias{distrib_quantile.LogisticDistrib}
\title{Logistic Quantile Function}
\arguments{
\item{distrib}{A \code{LogisticDistrib} object, from \code{\link[=logistic_distrib]{logistic_distrib()}}.}

\item{p}{A numeric vector of probabilities in \eqn{[0, 1]}, or of their
logarithms when \code{log.p = TRUE}. A value outside the range gives \code{NaN}
with a warning; the endpoints give \code{-Inf} and \code{Inf}.}

\item{theta}{A named list with components \code{mu} and \code{sigma}, each a numeric
vector of length 1 or of the length of \code{p}. A component of length 1 is
recycled. \code{sigma} must be strictly positive.}

\item{lower.tail}{Logical of length 1. When \code{TRUE}, the default, \code{p} is
\eqn{P(Y \le q)}; when \code{FALSE} it is \eqn{P(Y > q)}.}

\item{log.p}{Logical of length 1. When \code{TRUE} the values in \code{p} are read as
logarithms of probabilities. Defaults to \code{FALSE}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of quantiles, of length
\code{max(length(p), length(mu), length(sigma))}.
}
\description{
Computes the logistic quantile function
\deqn{Q(p; \mu, \sigma) = \mu + \sigma \log\left(\dfrac{p}{1-p}\right)}
by calling \code{\link[stats:Logistic]{stats::qlogis()}}. The argument of the logarithm is the odds, so
this function is the logit link applied to \code{p} and then carried onto the
location and scale.
}
\examples{
d <- logistic_distrib()
th <- list(mu = 0.4, sigma = 1.5)

# The median is mu, the family being symmetric.
distrib_quantile(d, c(0.25, 0.5, 0.75), th)

# Exact inverse: the round trip returns the probabilities it was given.
p <- c(0.025, 0.5, 0.975)
all.equal(distrib_cdf(d, distrib_quantile(d, p, th), th), p)

# It is mu + sigma times the log odds.
all.equal(distrib_quantile(d, p, th), 0.4 + 1.5 * log(p / (1 - p)))
}
\seealso{
\code{\link[=distrib_cdf.LogisticDistrib]{distrib_cdf.LogisticDistrib()}}, which this inverts;
\code{\link[linkfunctions7:logit_link]{linkfunctions7::logit_link()}}, which it applies to \code{p}; and
\code{\link[=distrib_quantile]{distrib_quantile()}} for the generic.
}
