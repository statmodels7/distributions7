% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/laplace_distrib.R
\name{kink_decomposition.LaplaceDistrib}
\alias{kink_decomposition.LaplaceDistrib}
\title{The Laplace's Kink}
\arguments{
\item{distrib}{A \code{LaplaceDistrib} object.}
}
\value{
A \code{\link[=kink_spec]{kink_spec()}} with \code{phi = "abs"}.
}
\description{
The log-density is \eqn{-\log(2\sigma) - \lvert y - \mu \rvert / \sigma}, so
the composition is the absolute value at \eqn{v = y - \mu} with
\eqn{c(\theta) = -1/\sigma}.
}
\details{
Only the location moves the kink: \eqn{\partial v/\partial \mu = -1} and
\eqn{\partial v/\partial \sigma = 0}, so \code{\link[=params_order]{params_order()}} reports
\eqn{m_\mu = 0} and \eqn{m_\sigma = \infty}, which is what \code{params_smooth}
has recorded as \code{c(mu = FALSE, sigma = TRUE)} all along.

The scale is smooth even though it stands in front of the absolute value,
because a coefficient multiplying \eqn{\phi} is differentiable wherever
\eqn{\phi} is bounded; what breaks a derivative is the argument crossing the
origin, not the factor.
}
\examples{
kink_decomposition(laplace_distrib())
params_order(laplace_distrib())
check_kink(laplace_distrib())

}
\seealso{
\code{\link[=kink_decomposition]{kink_decomposition()}} for the generic, \code{\link[=laplace_distrib]{laplace_distrib()}} for the
family.
}
\keyword{internal}
