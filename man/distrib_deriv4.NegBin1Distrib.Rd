% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/negbin1_higher.R
\name{distrib_deriv4.NegBin1Distrib}
\alias{distrib_deriv4.NegBin1Distrib}
\title{NB1 Fourth-Order Derivatives}
\arguments{
\item{distrib}{A \code{NegBin1Distrib} object, from \code{\link[=negbin1_distrib]{negbin1_distrib()}}.}

\item{y}{A numeric vector of counts. With \code{expected = TRUE} only its length
is read.}

\item{theta}{A named list with components \code{mu} and \code{theta}, each a numeric
vector of length 1 or of the length of \code{y}, both strictly positive. A
component of length 1 is recycled.}

\item{expected}{Logical of length 1. When \code{TRUE} the expectation under the
model is returned in place of the value at the data, computed numerically.
Defaults to \code{FALSE}.}

\item{scale}{One of \code{"parameter"} (the default) or \code{"link"}, matched by
\code{\link[base:match.arg]{base::match.arg()}}. Read by the generic, not by this method.}

\item{approx}{One of \code{"integrate"} (the default here), \code{"bartlett"}, \code{"mc"}
or \code{"opg"}. Read only when \code{expected = TRUE}.}

\item{nsim}{A single positive integer, the sample size when
\code{approx = "mc"}. Read only when \code{expected = TRUE}. Defaults to \code{10000}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A named list of five numeric vectors, \code{mu_mu_mu_mu},
\code{mu_mu_mu_theta}, \code{mu_mu_theta_theta}, \code{mu_theta_theta_theta} and
\code{theta_theta_theta_theta}, each of length
\code{max(length(y), lengths(theta))}.
}
\description{
Computes the five distinct fourth derivatives of the NB1 log-likelihood in
\eqn{\mu} and \eqn{\theta}, \strong{in closed form}, by the construction
\code{\link[=distrib_deriv3.NegBin1Distrib]{distrib_deriv3.NegBin1Distrib()}} describes carried one order further: the
same coefficient recursion over the powers of \eqn{r = \mu/\theta} and the
order of \eqn{G(r) = \log\Gamma(y+r) - \log\Gamma(r)}.

With \code{expected = TRUE} the method calls \code{\link[=expected_derivative]{expected_derivative()}} instead: the
expected fourth derivatives have no closed form.
}
\section{Notation}{

\eqn{\ell} is the log-likelihood of one observation, \eqn{\mu > 0} the mean,
\eqn{\theta > 0} the dispersion, \eqn{r = \mu/\theta} the negative binomial
size and \eqn{G(r) = \log\Gamma(y+r) - \log\Gamma(r)}.
}

\section{The Poisson boundary}{

The caveat of \code{\link[=distrib_deriv3.NegBin1Distrib]{distrib_deriv3.NegBin1Distrib()}} applies here and more
strongly: the recursion divides by \eqn{\theta^{4}}, so the cancellation
among the powers of \eqn{r} is worse at this order than at the one below.
This order is not reliable below about \eqn{\theta = 0.05}.
}

\examples{
d <- negbin1_distrib()
y <- c(0, 3, 7)
th <- list(mu = 4, theta = 1.2)
d4 <- distrib_deriv4(d, y, th)
names(d4)

# A central difference of the third order reproduces a mixed component.
eps <- 1e-4
up <- distrib_deriv3(d, y, list(mu = 4, theta = 1.2 + eps))$mu_mu_theta
dn <- distrib_deriv3(d, y, list(mu = 4, theta = 1.2 - eps))$mu_mu_theta
all.equal((up - dn) / (2 * eps), d4$mu_mu_theta_theta, tolerance = 1e-5)
}
\seealso{
\code{\link[=distrib_deriv3.NegBin1Distrib]{distrib_deriv3.NegBin1Distrib()}} for the order below and the
recursion, \code{\link[=negbin1_components]{negbin1_components()}} for the assembly, and
\code{\link[=distrib_deriv4]{distrib_deriv4()}} for the generic.
}
