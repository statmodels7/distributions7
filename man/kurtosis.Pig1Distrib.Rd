% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{kurtosis.Pig1Distrib}
\alias{kurtosis.Pig1Distrib}
\title{Excess Kurtosis of the Poisson-Inverse Gaussian Distribution}
\arguments{
\item{x}{A \code{Pig1Distrib}, from \code{\link[=pig1_distrib]{pig1_distrib()}}.}

\item{theta}{A named list with components \code{mu} (positive) and \code{sigma}
(positive), each a numeric vector of length 1 or \code{n}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of excess kurtoses, of length
\code{max(length(theta$mu), length(theta$sigma))}, positive throughout.
}
\description{
Closed form from the cumulants, \eqn{\gamma_2 = \kappa_4/\kappa_2^{2}} with
\deqn{\kappa_2 = \mu + \sigma\mu^2, \qquad
      \kappa_4 = \mu + 7\sigma\mu^2 + 18\sigma^2\mu^3 + 15\sigma^3\mu^4.}
The ratio of the fourth cumulant to the squared second \strong{is} the excess
kurtosis, so no 3 is subtracted here: the subtraction is what turns a fourth
\emph{moment} into an excess, and a fourth cumulant already carries it.
}
\section{Notation}{

\eqn{\mu > 0} is the mean, \eqn{\sigma > 0} the dispersion and
\eqn{\kappa_k} the \eqn{k}-th cumulant.
}

\examples{
d <- pig1_distrib()

# The cumulant ratio, written out.
all.equal(kurtosis(d, list(mu = 3, sigma = 1)),
          (3 + 7 * 9 + 18 * 27 + 15 * 81) / 144)

# It falls onto the Poisson's 1 / mu as the dispersion vanishes.
c(pig = kurtosis(d, list(mu = 3, sigma = 1e-9)),
  poisson = kurtosis(poisson_distrib(), list(mu = 3)))

}
\seealso{
\code{\link[=skewness.Pig1Distrib]{skewness.Pig1Distrib()}}, from the third cumulant;
\code{\link[=kurtosis.NegBin2Distrib]{kurtosis.NegBin2Distrib()}}, the comparison;
\code{\link[=kurtosis.PoissonDistrib]{kurtosis.PoissonDistrib()}}, the limit; \code{\link[=pig1_distrib]{pig1_distrib()}}.
}
\keyword{internal}
