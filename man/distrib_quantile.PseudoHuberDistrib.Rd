% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pseudohuber_distrib.R
\name{distrib_quantile.PseudoHuberDistrib}
\alias{distrib_quantile.PseudoHuberDistrib}
\title{Pseudo-Huber Quantile Function}
\arguments{
\item{distrib}{A \code{PseudoHuberDistrib} object, from
\code{\link[=pseudohuber_distrib]{pseudohuber_distrib()}}.}

\item{p}{A numeric vector of probabilities in \eqn{[0, 1]}, or of their
logarithms when \code{log.p = TRUE}. \code{NaN} is returned for \code{NA} and for a value
outside the range; 0 gives \code{-Inf} and 1 gives \code{Inf}.}

\item{theta}{A named list with components \code{mu}, \code{sigma} and \code{nu}, each a
numeric vector of length 1 or of the length of \code{p}. A component of length
1 is recycled. \code{sigma} and \code{nu} must be strictly positive.}

\item{lower.tail}{Logical of length 1. When \code{TRUE}, the default, \code{p} is
\eqn{P(Y \le q)}; when \code{FALSE} it is \eqn{P(Y > q)}.}

\item{log.p}{Logical of length 1. When \code{TRUE}, \code{p} is read as a logarithm.
Defaults to \code{FALSE}.}
}
\value{
A numeric vector of quantiles on \eqn{[-\infty, \infty]}, of length
\code{max(length(p), length(mu), length(sigma), length(nu))}.
}
\description{
Computes \eqn{Q(p)} by root-finding on the numerical distribution function,
the family having no elementary quantile. Symmetry about \eqn{\mu} does most
of the work: \eqn{Q(1/2) = \mu} exactly and without a search, and a
probability above one half is reflected through
\eqn{Q(p) = 2\mu - Q(1-p)}, so the search always runs in the lower half.

The bracket starts at ten standard deviations below the location and doubles
its width until it contains the root, at most 100 times.
\code{\link[stats:uniroot]{stats::uniroot()}} then closes it to \code{sqrt(.Machine$double.eps)}. Each
evaluation of the objective is a quadrature, so this is by far the dearest
method of the family.
}
\examples{
d <- pseudohuber_distrib()
th <- list(mu = 0.4, sigma = 1.2, nu = 2)

# The median is the location, returned without a search.
distrib_quantile(d, 0.5, th)

# The quartiles, and the round trip back through the distribution function.
q <- distrib_quantile(d, c(0.25, 0.75), th)
q
distrib_cdf(d, q, th)

# Symmetry: the two quartiles are equidistant from the location.
q - 0.4
}
\seealso{
\code{\link[=distrib_cdf.PseudoHuberDistrib]{distrib_cdf.PseudoHuberDistrib()}} for the function inverted here,
\code{\link[=distrib_rng.PseudoHuberDistrib]{distrib_rng.PseudoHuberDistrib()}}, which draws by inverting it at uniform
variates, and \code{\link[=distrib_quantile]{distrib_quantile()}} for the generic.
}
