% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpd_higher.R
\name{distrib_deriv4.GPDDistrib}
\alias{distrib_deriv4.GPDDistrib}
\title{Generalized Pareto Fourth-Order Derivatives}
\arguments{
\item{distrib}{A \code{GPDDistrib} object, from \code{\link[=gpd_distrib]{gpd_distrib()}}.}

\item{y}{A numeric vector of observations on the support. With
\code{expected = TRUE} only its length is read.}

\item{theta}{A named list with components \code{sigma} and \code{xi}, each a numeric
vector of length 1 or of the length of \code{y}. \code{sigma} must be strictly
positive; \code{xi} may be of either sign, including zero.}

\item{expected}{Logical of length 1. When \code{TRUE} the expectation under the
model is returned in place of the value at the data, computed numerically.
Defaults to \code{FALSE}.}

\item{scale}{One of \code{"parameter"} (the default) or \code{"link"}, matched by
\code{\link[base:match.arg]{base::match.arg()}}. Read by the generic, not by this method.}

\item{approx}{One of \code{"integrate"} (the default here), \code{"bartlett"}, \code{"mc"}
or \code{"opg"}. Read only when \code{expected = TRUE}.}

\item{nsim}{A single positive integer, the sample size when
\code{approx = "mc"}. Read only when \code{expected = TRUE}. Defaults to \code{10000}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}

\item{threads}{A single positive integer, how many threads the polynomial
kernel of the series branch may use. Defaults to \code{1L}.}
}
\value{
A named list of five numeric vectors, \code{sigma_sigma_sigma_sigma},
\code{sigma_sigma_sigma_xi}, \code{sigma_sigma_xi_xi}, \code{sigma_xi_xi_xi} and
\code{xi_xi_xi_xi}, each of length \code{max(length(y), lengths(theta))}.
}
\description{
Computes the five distinct fourth derivatives of the generalized Pareto
log-density in \eqn{\sigma} and \eqn{\xi}, \strong{in closed form}, by the
construction \code{\link[=distrib_deriv3.GPDDistrib]{distrib_deriv3.GPDDistrib()}} describes carried one order
further. This is the order at which the choice between the two routes bites
hardest: the Leibniz form's relative cancellation is \eqn{(\xi z)^{-b}}, so
at \eqn{b = 4} it has lost a third of the answer by
\eqn{\xi z = 1.7\times10^{-4}} and everything below that.

With \code{expected = TRUE} the method calls \code{\link[=expected_derivative]{expected_derivative()}} instead: the
expected fourth derivatives have no closed form.
}
\section{Notation}{

\eqn{\ell} is the log-density of one observation, \eqn{\sigma > 0} the
scale, \eqn{\xi} the shape, \eqn{z = y/\sigma} and \eqn{t = 1 + \xi z}.
}

\examples{
d <- gpd_distrib()
y <- c(0.5, 2, 8)
th <- list(sigma = 1.5, xi = 0.3)
d4 <- distrib_deriv4(d, y, th)
names(d4)

# The two routes agree wherever both are accurate: forcing the cut either
# way at xi * z = 0.05, 0.15 and 0.30.
gap <- function(xz) {
  xi <- xz / (2 / 1.5)
  a <- gpd_components(2, list(sigma = 1.5, xi = xi), 4L, cut = 1e-9)
  b <- gpd_components(2, list(sigma = 1.5, xi = xi), 4L, cut = 10)
  max(abs(unlist(a) - unlist(b)) / pmax(abs(unlist(a)), 1e-10))
}
vapply(c(0.05, 0.15, 0.30), gap, numeric(1))

# And below the cut the Leibniz form loses the answer entirely, which is
# what the series branch exists for.
leib <- function(x)
  gpd_components(2, list(sigma = 1.5, xi = x), 4L, cut = 1e-12)$xi_xi_xi_xi
ser <- function(x)
  gpd_components(2, list(sigma = 1.5, xi = x), 4L, cut = 10)$xi_xi_xi_xi
rbind(xi = c(1e-2, 1e-4, 1e-6),
      leibniz = vapply(c(1e-2, 1e-4, 1e-6), leib, numeric(1)),
      series = vapply(c(1e-2, 1e-4, 1e-6), ser, numeric(1)))
}
\seealso{
\code{\link[=distrib_deriv3.GPDDistrib]{distrib_deriv3.GPDDistrib()}} for the order below and the
construction, \code{\link[=gpd_components]{gpd_components()}} for the two routes and the measured
threshold, and \code{\link[=distrib_deriv4]{distrib_deriv4()}} for the generic.
}
