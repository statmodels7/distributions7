% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/starting_values.R
\name{clamp_to_bounds}
\alias{clamp_to_bounds}
\title{Move a Starting Value Strictly Inside the Parameter Bounds}
\usage{
clamp_to_bounds(theta, distrib)
}
\arguments{
\item{theta}{A named list of parameters on the parameter scale. Components
with no entry in \code{params_bounds}, and bounds that are not a pair, are left
alone.}

\item{distrib}{The distribution whose \code{params_bounds} apply.}
}
\value{
\code{theta}, each component strictly inside its own bounds.
}
\description{
Returns \code{theta} with every component moved strictly inside its own bounds.
A moment estimate can land exactly on a boundary. A sample of non-negative
counts gives a median of zero and a sample of proportions a maximum of one,
and \code{align_theta()} treats \code{params_bounds} as \strong{open}, so such a value is
rejected by the first generic that sees it.

A value at a finite non-zero bound is moved in by 16 machine epsilons
relative to the bound. A value at a bound of exactly zero cannot be, there
being no relative scale there, so it is replaced by \eqn{10^{-8}} times the
larger of the value's own magnitude and 1. Both are far below anything a
first optimizer step resolves.
}
\seealso{
\code{\link[=start_from_moments]{start_from_moments()}} and \code{\link[=moment_estimates]{moment_estimates()}}, the callers;
\code{\link[=align_theta]{align_theta()}}, which enforces the open bounds this exists to satisfy.
}
\keyword{internal}
