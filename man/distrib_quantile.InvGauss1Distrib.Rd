% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/invgauss1_distrib.R
\name{distrib_quantile.InvGauss1Distrib}
\alias{distrib_quantile.InvGauss1Distrib}
\title{Inverse Gaussian Quantile Function in Mean and Dispersion}
\arguments{
\item{distrib}{An \code{InvGauss1Distrib} object, from \code{\link[=invgauss1_distrib]{invgauss1_distrib()}}.}

\item{p}{A numeric vector of probabilities in \eqn{[0, 1]}, or of their
logarithms when \code{log.p = TRUE}. A value outside the range gives \code{NaN}
with a warning; \code{p = 0} gives 0 and \code{p = 1} gives \code{Inf}.}

\item{theta}{A named list with components \code{mu} and \code{phi}, each a numeric
vector of length 1 or of the length of \code{p}. A component of length 1 is
recycled. Both must be strictly positive.}

\item{lower.tail}{Logical of length 1. When \code{TRUE}, the default, \code{p} is
\eqn{P(Y \le q)}; when \code{FALSE} it is \eqn{P(Y > q)}.}

\item{log.p}{Logical of length 1. When \code{TRUE} the values in \code{p} are read as
logarithms of probabilities, which is how a quantile deep in a tail is
requested without the probability underflowing. Defaults to \code{FALSE}.}
}
\value{
A numeric vector of quantiles in \eqn{[0, \infty]}, of length
\code{max(length(p), length(mu), length(phi))}.
}
\description{
Computes the inverse Gaussian quantile function by calling
\code{\link[statmod:invgauss]{statmod::qinvgauss()}} at \code{mean = mu} and \code{dispersion = phi}. There is no
closed form: the distribution function is elementary but not invertible in
elementary terms, so \code{qinvgauss()} inverts it numerically. The distribution
function is strictly increasing on \eqn{(0, \infty)}, so the round trip
through \code{\link[=distrib_cdf.InvGauss1Distrib]{distrib_cdf.InvGauss1Distrib()}} returns \code{p}.
}
\examples{
d <- invgauss1_distrib()
th <- list(mu = 1, phi = 2)

# A central 95 percent interval, extremely asymmetric about the mean of 1.
distrib_quantile(d, c(0.025, 0.5, 0.975), th)

# Exact inverse: the round trip returns the probabilities it was given.
p <- c(0.025, 0.5, 0.975)
all.equal(distrib_cdf(d, distrib_quantile(d, p, th), th), p)

# The median falls well below the mean at this dispersion.
c(median = distrib_quantile(d, 0.5, th), mean = mean(d, th))
}
\seealso{
\code{\link[=distrib_cdf.InvGauss1Distrib]{distrib_cdf.InvGauss1Distrib()}}, which this inverts;
\code{\link[=distrib_rng.InvGauss1Distrib]{distrib_rng.InvGauss1Distrib()}}, which does not use it;
\code{\link[=distrib_quantile]{distrib_quantile()}} for the generic.
}
