% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdf_derivatives_families.R
\name{distrib_hess_cdf.Gaussian3Distrib}
\alias{distrib_hess_cdf.Gaussian3Distrib}
\title{Gaussian Log-CDF Hessian in Mean and Precision}
\arguments{
\item{distrib}{A \code{Gaussian3Distrib} object, from \code{\link[=gaussian3_distrib]{gaussian3_distrib()}}.}

\item{q}{A numeric vector of quantiles.}

\item{theta}{A named list with components \code{mu} and \code{tau} (positive), each a
numeric vector of length 1 or \code{n}.}

\item{lower.tail}{Is the lower tail wanted? A single logical, \code{TRUE} by
default.}

\item{log}{Are derivatives of the log probability wanted? A single logical,
\code{TRUE} by default.}
}
\value{
A named list of three numeric vectors keyed as \code{\link[=hess_names]{hess_names()}}, each
the length of \code{q} recycled against \code{theta}.
}
\description{
Closed form, by the second-order chain rule through
\eqn{\sigma = \tau^{-1/2}}. The map's second partial
\eqn{\partial^2\sigma/\partial\tau^2 = \tfrac34\tau^{-5/2}} contributes to
the precision-precision component.
}
\section{Notation}{

\eqn{\mu} is the mean, \eqn{\tau > 0} the precision and \eqn{F} the
distribution function.
}

\examples{
q <- c(-1, 0.5, 2)
th <- list(mu = 0.3, tau = 1 / 1.44)

# Against a central difference of the cdf, which shares no arithmetic.
exact <- distrib_hess_cdf(gaussian3_distrib(), q, th, log = FALSE)
fd <- numerical_cdf_deriv(gaussian3_distrib(), q, th, order = 2)
max(abs(unlist(exact[names(fd)]) / unlist(fd) - 1))

}
\seealso{
\code{\link[=distrib_grad_cdf.Gaussian3Distrib]{distrib_grad_cdf.Gaussian3Distrib()}} for the first order;
\code{\link[=chain_cdf_deriv]{chain_cdf_deriv()}} for the identity; \code{\link[=gaussian3_distrib]{gaussian3_distrib()}}.
}
\keyword{internal}
