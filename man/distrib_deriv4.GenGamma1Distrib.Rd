% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gengamma1_higher.R
\name{distrib_deriv4.GenGamma1Distrib}
\alias{distrib_deriv4.GenGamma1Distrib}
\title{Generalized Gamma Fourth-Order Derivatives}
\arguments{
\item{distrib}{A \code{GenGamma1Distrib} object, from \code{\link[=gengamma1_distrib]{gengamma1_distrib()}}.}

\item{y}{A numeric vector of positive observations. With \code{expected = TRUE}
only its length is read.}

\item{theta}{A named list with components \code{a}, \code{d} and \code{p}, each a numeric
vector of length 1 or of the length of \code{y}, all strictly positive. A
component of length 1 is recycled.}

\item{expected}{Logical of length 1. When \code{TRUE} the expectation under the
model is returned in place of the value at the data, computed numerically.
Defaults to \code{FALSE}.}

\item{scale}{One of \code{"parameter"} (the default) or \code{"link"}, matched by
\code{\link[base:match.arg]{base::match.arg()}}. Read by the generic, not by this method.}

\item{approx}{One of \code{"integrate"} (the default here), \code{"bartlett"}, \code{"mc"}
or \code{"opg"}. Read only when \code{expected = TRUE}.}

\item{nsim}{A single positive integer, the sample size when
\code{approx = "mc"}. Read only when \code{expected = TRUE}. Defaults to \code{10000}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A named list of fifteen numeric vectors named for the multi-index
they carry, from \code{a_a_a_a} to \code{p_p_p_p}, each of length
\code{max(length(y), lengths(theta))}.
}
\description{
Computes the fifteen distinct fourth derivatives of the generalized gamma
log-density in \eqn{a}, \eqn{d} and \eqn{p}, \strong{in closed form}, by the
construction \code{\link[=distrib_deriv3.GenGamma1Distrib]{distrib_deriv3.GenGamma1Distrib()}} describes carried one order
further: the two two-variable compositions of \code{\link[=fdb2]{fdb2()}} plus the elementary
pieces of the log-density.

With \code{expected = TRUE} the method calls \code{\link[=expected_derivative]{expected_derivative()}} instead: the
expected fourth derivatives have no closed form. That is the one place on
this page where \code{approx} and \code{nsim} are read.
}
\section{Notation}{

\eqn{\ell} is the log-density of one observation, \eqn{a > 0} the scale,
\eqn{d > 0} and \eqn{p > 0} the two shapes.
}

\examples{
d <- gengamma1_distrib()
y <- c(0.6, 1.4, 3.1)
th <- list(a = 2, d = 1.5, p = 1.3)
d4 <- distrib_deriv4(d, y, th)
length(d4)
names(d4)[1:4]

# A central difference of the third order reproduces the pure-scale
# component.
eps <- 1e-4
up <- distrib_deriv3(d, y, list(a = 2 + eps, d = 1.5, p = 1.3))$a_a_a
dn <- distrib_deriv3(d, y, list(a = 2 - eps, d = 1.5, p = 1.3))$a_a_a
all.equal((up - dn) / (2 * eps), d4$a_a_a_a, tolerance = 1e-5)
}
\seealso{
\code{\link[=distrib_deriv3.GenGamma1Distrib]{distrib_deriv3.GenGamma1Distrib()}} for the order below and the
construction, \code{\link[=gengamma_components]{gengamma_components()}} for the assembly, and
\code{\link[=distrib_deriv4]{distrib_deriv4()}} for the generic.
}
