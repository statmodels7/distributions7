% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{kurtosis.InvGauss2Distrib}
\alias{kurtosis.InvGauss2Distrib}
\title{Excess Kurtosis of the Inverse Gaussian Distribution in Mean and Rate}
\arguments{
\item{x}{An \code{InvGauss2Distrib}, from \code{\link[=invgauss2_distrib]{invgauss2_distrib()}}.}

\item{theta}{A named list with components \code{mu} (positive) and \code{lambda}
(positive), each a numeric vector of length 1 or \code{n}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of excess kurtoses, of length
\code{max(length(theta$mu), length(theta$lambda))}, positive throughout.
}
\description{
Closed form: \eqn{\gamma_2 = 15\mu/\lambda}, the excess over the Gaussian.
Like the skewness it depends on the two parameters through their ratio
alone, and it is exactly \eqn{5\gamma_1^2/3}, so the family occupies one
curve of the skewness-kurtosis plane, as it does in the other
parametrization.
}
\section{Notation}{

\eqn{\mu > 0} is the mean and \eqn{\lambda > 0} the rate.
}

\examples{
d <- invgauss2_distrib()

# Fifteen times the ratio.
all.equal(kurtosis(d, list(mu = 2, lambda = 8)), 3.75)

# The family lies on the curve kurtosis = (5/3) skewness^2.
th <- list(mu = 2, lambda = 8)
all.equal(kurtosis(d, th), (5 / 3) * skewness(d, th)^2)

}
\seealso{
\code{\link[=skewness.InvGauss2Distrib]{skewness.InvGauss2Distrib()}}, to which this is tied;
\code{\link[=kurtosis.InvGauss1Distrib]{kurtosis.InvGauss1Distrib()}}; \code{\link[=kurtosis.Gamma1Distrib]{kurtosis.Gamma1Distrib()}}, whose constant
is \eqn{3/2}; \code{\link[=invgauss2_distrib]{invgauss2_distrib()}}.
}
\keyword{internal}
