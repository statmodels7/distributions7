% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/laplace2_distrib.R
\name{distrib_cdf.Laplace2Distrib}
\alias{distrib_cdf.Laplace2Distrib}
\title{Laplace Cumulative Distribution Function, Rate Parametrization}
\arguments{
\item{distrib}{A \code{Laplace2Distrib} object, from \code{\link[=laplace2_distrib]{laplace2_distrib()}}.}

\item{q}{A numeric vector of quantiles.}

\item{theta}{A named list with components \code{mu} and \code{lambda}, each a numeric
vector of length 1 or of the length of \code{q}. A component of length 1 is
recycled. \code{lambda} must be strictly positive.}

\item{lower.tail}{Logical of length 1. When \code{TRUE}, the default,
probabilities are \eqn{P(Y \le q)}; when \code{FALSE} they are \eqn{P(Y > q)},
computed as \eqn{1 - F}.}

\item{log.p}{Logical of length 1. When \code{TRUE} the logarithm of the
probability is returned. Defaults to \code{FALSE}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of probabilities in \eqn{[0, 1]}, of length
\code{max(length(q), length(mu), length(lambda))}.
}
\description{
Computes the Laplace distribution function in the rate parametrization,
one exponential on each side of the location:
\deqn{F(q; \mu, \lambda) = \begin{cases}
  \tfrac{1}{2}\exp(\lambda(q-\mu)), & q < \mu,\\[4pt]
  1 - \tfrac{1}{2}\exp(-\lambda(q-\mu)), & q \ge \mu.
\end{cases}}
Both arms are continuous at \eqn{q = \mu}, where the value is \eqn{1/2}.
With \code{lower.tail = FALSE} the complement is taken after the branch, and with
\code{log.p = TRUE} the logarithm is taken afterwards; neither avoids
cancellation, so a probability that has already underflowed is not
recovered.
}
\examples{
d <- laplace2_distrib()
th <- list(mu = 0.4, lambda = 2)

# One half at the location.
distrib_cdf(d, 0.4, th)

# Each arm is an exponential, written out.
q <- c(-2, 3)
all.equal(distrib_cdf(d, q, th),
          ifelse(q < 0.4, 0.5 * exp(2 * (q - 0.4)),
                 1 - 0.5 * exp(-2 * (q - 0.4))))

# The same probabilities as the scale parametrization at sigma = 1/lambda.
all.equal(distrib_cdf(d, q, list(mu = 0.4, lambda = 1 / 1.5)),
          distrib_cdf(laplace_distrib(), q, list(mu = 0.4, sigma = 1.5)))
}
\seealso{
\code{\link[=distrib_cdf.LaplaceDistrib]{distrib_cdf.LaplaceDistrib()}} for the scale parametrization,
\code{\link[=distrib_quantile.Laplace2Distrib]{distrib_quantile.Laplace2Distrib()}} for the inverse, and \code{\link[=distrib_cdf]{distrib_cdf()}}
for the generic.
}
