% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/y_higher.R
\name{distrib_deriv3_y.ReparamContinuousDistrib}
\alias{distrib_deriv3_y.ReparamContinuousDistrib}
\alias{distrib_deriv4_y.ReparamContinuousDistrib}
\title{Higher Response Derivatives of a Reparametrized Distribution}
\arguments{
\item{distrib}{A \code{ReparamContinuousDistrib} object, from \code{\link[=reparametrize]{reparametrize()}}.}

\item{y}{A numeric vector of observations.}

\item{theta}{A named list of parameters of the \strong{new} parametrization,
carried to the parent's by \code{\link[=reparam_theta]{reparam_theta()}} before the call.}

\item{...}{Passed to the parent's method.}
}
\value{
A numeric vector of length \code{length(y)}: the parent's derivative of
that order at the mapped parameters.
}
\description{
The parent's third and fourth response derivatives, read at the parent's
parameters. A reparametrization acts on \eqn{\theta} and these derivatives
are taken in \eqn{y}, so the two do not interact: no chain rule enters and
the parent's answer is the answer, exactly.

The same is true at the first and second orders, and for the mixed block
\code{\link[=distrib_cross_y]{distrib_cross_y()}} it is \strong{not}: that one takes one derivative in each,
so the map's first-order Jacobian does enter.
}
\seealso{
\code{\link[=reparametrize]{reparametrize()}} for the wrapper;
\code{\link[=distrib_cross_y.ReparamContinuousDistrib]{distrib_cross_y.ReparamContinuousDistrib()}}, where the map does enter;
\code{\link[=distrib_deriv3_y]{distrib_deriv3_y()}} for the generic.
}
\keyword{internal}
