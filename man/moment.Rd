% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{moment}
\alias{moment}
\title{Raw and Central Moments of a Distribution}
\usage{
moment(distrib, theta, p = 1, central = FALSE, mu = NULL, ...)
}
\arguments{
\item{distrib}{An object inheriting from class \code{"distrib"}.}

\item{theta}{A named list of parameters. Vectors are supported (vectorized computation).}

\item{p}{Numeric. The order of the moment. Can be a vector (recycled against \code{theta}).}

\item{central}{Logical. If \code{TRUE}, computes the central moment \eqn{E[(Y-\mu)^p]},
where \eqn{\mu = E[Y]} (computed numerically unless \code{mu} is supplied).}

\item{mu}{Optional numeric. The centering value(s) used when \code{central = TRUE}.
If \code{NULL}, the mean is computed numerically.}

\item{...}{Additional arguments passed to \code{\link{expectation}}.}
}
\value{
A numeric vector of moments, with length equal to the maximum length
among \code{theta} components and \code{p}.
}
\description{
Computes raw moments \eqn{E[Y^p]} or central moments \eqn{E[(Y-\mu)^p]} of a
distribution numerically, via \code{\link{expectation}} (numerical integration
for continuous distributions, series summation for discrete ones).
}
\examples{
\dontrun{
d <- gaussian1_distrib()
moment(d, list(mu = 2, sigma = 3), p = 1)                 # 2
moment(d, list(mu = 2, sigma = 3), p = 2, central = TRUE) # 9
}

}
\seealso{
\code{\link{expectation}}, \code{\link{variance}}, \code{\link{std_dev}}, \code{\link{skewness}}, \code{\link{kurtosis}}
}
