% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{kurtosis.SkewTDistrib}
\alias{kurtosis.SkewTDistrib}
\title{Excess Kurtosis of the Skew t Distribution}
\arguments{
\item{x}{A \code{SkewTDistrib}, from \code{\link[=skewt_distrib]{skewt_distrib()}}.}

\item{theta}{A named list with components \code{mu}, \code{sigma} (positive), \code{alpha}
(any sign) and \code{nu} (positive), each a numeric vector of length 1 or \code{n}.
Only \code{alpha} and \code{nu} enter the value; settings with \eqn{\nu \le 4} give
\code{NaN}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of excess kurtoses, as long as the longer of
\code{alpha} and \code{nu}, \code{NaN} wherever \eqn{\nu \le 4}. Neither the location nor
the scale enters the value or lengthens it.
}
\description{
Closed form for \eqn{\nu > 4}, from Azzalini and Capitanio (2003):
\deqn{\gamma_2 = \frac{1}{\sigma_z^{4}}
      \left\{\frac{3\nu^2}{(\nu-2)(\nu-4)}
             - \frac{4\mu_z^2\nu(3-\delta^2)}{\nu-3}
             + \frac{6\mu_z^2\nu}{\nu-2} - 3\mu_z^4\right\} - 3,}
and \code{NaN} at \eqn{\nu \le 4}. The threshold is the highest of the four
moments, the fourth being the one that fails first as the tails grow heavy.
}
\section{Notation}{

\eqn{\delta = \alpha/\sqrt{1+\alpha^2}}, \eqn{\mu_z = \delta b_\nu} and
\eqn{\sigma_z^2 = \nu/(\nu-2) - \mu_z^2} are the standardized mean and
variance from \code{\link[=skewt_moment_pieces]{skewt_moment_pieces()}}.
}

\examples{
d <- skewt_distrib()

# At shape zero this is the Student t's 6 / (nu - 4).
kurtosis(d, list(mu = 0, sigma = 1, alpha = 0, nu = c(4, 5, 10)))

# As nu grows it approaches the skew normal's value at the same shape.
c(skewt = kurtosis(d, list(mu = 0, sigma = 1, alpha = 3, nu = 1e6)),
  skewnormal = kurtosis(skewnormal1_distrib(),
                        list(mu = 0, sigma = 1, alpha = 3)))

}
\references{
Azzalini, A. and Capitanio, A. (2003). Distributions generated by
perturbation of symmetry with emphasis on a multivariate skew t
distribution. \emph{Journal of the Royal Statistical Society Series B} \strong{65},
367-389.
}
\seealso{
\code{\link[=skewness.SkewTDistrib]{skewness.SkewTDistrib()}}, whose threshold is 3;
\code{\link[=kurtosis.SkewNormal1Distrib]{kurtosis.SkewNormal1Distrib()}}, the bounded limit;
\code{\link[=skewt_moment_pieces]{skewt_moment_pieces()}}, \code{\link[=skewt_distrib]{skewt_distrib()}}.
}
\keyword{internal}
