% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_scale.R
\name{inverse_link_derivs}
\alias{inverse_link_derivs}
\title{Inverse-Link Derivatives for Every Parameter}
\usage{
inverse_link_derivs(distrib, theta, order)
}
\arguments{
\item{distrib}{An object inheriting from class \code{"distrib"}.}

\item{theta}{A named list of parameters, on the natural scale.}

\item{order}{The highest derivative order needed, 1 to 4.}
}
\value{
A list with one element per parameter, each a list whose \eqn{k}-th
element is \eqn{h^{(k)}} evaluated at that parameter's \eqn{\eta}.
}
\description{
The derivatives \eqn{h', h'', h''', h''''} of each parameter's inverse link,
evaluated at \eqn{\eta = g(\theta)}, up to \code{order}.
}
\details{
This is the hot path of the link scale, so it is written against the
order-specific generics of \pkg{linkfunctions7} rather than the convenience
router; see the comment in the body for why, and \code{\link[=link_scale_derivatives]{link_scale_derivatives()}}
for what the derivatives are then used for.
}
\seealso{
\code{\link[=to_link_scale]{to_link_scale()}}
}
\keyword{internal}
