% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skewt_distrib.R
\name{distrib_expected_hessian.SkewTDistrib}
\alias{distrib_expected_hessian.SkewTDistrib}
\alias{distrib_dexpected_hessian.SkewTDistrib}
\alias{distrib_d2expected_hessian.SkewTDistrib}
\title{Skew t Expected Hessian and Its Derivatives}
\arguments{
\item{distrib}{A \code{SkewTDistrib} object, from \code{\link[=skewt_distrib]{skewt_distrib()}}.}

\item{y}{A numeric vector of observations. Its length sets the length of
each returned component; the values themselves are not read.}

\item{theta}{A named list with components \code{mu}, \code{sigma}, \code{alpha} and \code{nu},
each a numeric vector of length 1 or of the length of \code{y}.}

\item{scale}{One of \code{"parameter"} (the default) or \code{"link"}, matched by
\code{\link[base:match.arg]{base::match.arg()}}.}

\item{approx, nsim}{Ignored.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}

\item{threads}{The thread count passed to the family's kernels.}
}
\value{
A named list of numeric vectors of length \code{length(y)}: for
\code{\link[=distrib_expected_hessian]{distrib_expected_hessian()}} the components keyed as \code{\link[=hess_names]{hess_names()}}, for
the two derivatives those keyed as \code{\link[=dexpected_names]{dexpected_names()}} and
\code{\link[=d2expected_names]{d2expected_names()}}.
}
\description{
Returns the expected information, and through \code{\link[=distrib_dexpected_hessian]{distrib_dexpected_hessian()}}
and \code{\link[=distrib_d2expected_hessian]{distrib_d2expected_hessian()}} its first and second derivatives in the
parameters.
}
\details{
No component has an elementary form. The family is a location-scale family,
so every component equals its value at \eqn{\mu = 0}, \eqn{\sigma = 1} times
\eqn{\sigma^{-k}}, with \eqn{k} the number of indices on \eqn{\mu} or
\eqn{\sigma}. The value there depends on \eqn{(\alpha, \nu)} alone and is one
integral over \eqn{z} of the observed derivatives against the density, taken
once per distinct pair by the exp-sinh rule of \code{\link[=loc_scale_expected]{loc_scale_expected()}}.

The integral is exact to the rule's accuracy, and what it integrates carries
the family's own accuracy: every derivative in \eqn{(\mu, \sigma, \alpha)}
is closed form, and every one involving \eqn{\nu} comes from a single
stencil on an analytic quantity, as documented on
\code{\link[=distrib_hessian.SkewTDistrib]{distrib_hessian.SkewTDistrib()}}. Against an adaptive quadrature the
expected information agrees to \eqn{9\times10^{-12}}; the second derivative
agrees with a difference of the first to \eqn{10^{-3}} relative at
\eqn{\nu = 5}, the components carrying \eqn{\nu} several times being
differenced ones.

The tail is integrated to \eqn{|z| = 10^{60}}, which leaves a relative
\eqn{10^{-60\nu}/\nu} and is negligible for \eqn{\nu \ge 0.3}.

\code{approx} and \code{nsim} are accepted for the generic's sake and ignored.
}
\examples{
d <- skewt_distrib()
th <- list(mu = 1, sigma = 2, alpha = 2, nu = 5)
e <- distrib_expected_hessian(d, 0, th)
vapply(e, function(v) v[1], numeric(1))

# One quadrature serves every observation sharing a shape: the location and
# the scale only rescale the result.
e2 <- distrib_expected_hessian(d, 0, list(mu = -3, sigma = 1, alpha = 2, nu = 5))
e$alpha_alpha / e2$alpha_alpha
}
\seealso{
\code{\link[=loc_scale_expected]{loc_scale_expected()}} for the construction,
\code{\link[=distrib_hessian.SkewTDistrib]{distrib_hessian.SkewTDistrib()}} for the quantity this is the expectation
of, and \code{\link[=distrib_expected_hessian]{distrib_expected_hessian()}} for the generic.
}
