% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enet_higher.R
\name{distrib_deriv4.EnetDistrib}
\alias{distrib_deriv4.EnetDistrib}
\title{Elastic-Net Fourth Derivatives}
\arguments{
\item{distrib}{An \code{EnetDistrib} object, from \code{\link[=enet_distrib]{enet_distrib()}}.}

\item{y}{A numeric vector of observations. With \code{expected = TRUE} only its
length matters.}

\item{theta}{A named list with components \code{mu}, \code{lambda} and \code{alpha}, each a
numeric vector of length 1 or of the length of \code{y}.}

\item{expected}{Logical of length 1. When \code{FALSE}, the default, the observed
derivatives at \code{y} are returned.}

\item{scale}{Either \code{"parameter"}, the default, or \code{"link"}. The
transformation is applied in the generic's body.}

\item{approx}{One of \code{"integrate"}, \code{"bartlett"}, \code{"mc"} or \code{"opg"}, read
only when \code{expected = TRUE}.}

\item{nsim}{A single positive integer, the Monte Carlo sample size used when
\code{approx = "mc"}. Defaults to \code{10000}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A named list of fifteen numeric vectors, from \code{mu_mu_mu_mu} to
\code{alpha_alpha_alpha_alpha}.
}
\description{
Computes the fifteen fourth derivatives of the log-density in closed form,
through \code{\link[=.enet_chain]{.enet_chain()}}, in the notation of
\code{\link[=distrib_deriv3.EnetDistrib]{distrib_deriv3.EnetDistrib()}}. At this order \strong{nothing touches the
location}: the data term is quadratic in \eqn{z} and linear in each rate,
so every component naming \eqn{\mu} more than twice is exactly zero, and
what remains is a derivative of the normalizing constant carried through the
bilinear map.

With \code{expected = TRUE} the value is an expectation and is not closed form,
as at third order.
}
\examples{
d <- enet_distrib()
y <- c(-1.5, -0.3, 0.4, 2.1)
th <- list(mu = 0, lambda = 2, alpha = 0.5)
d4 <- distrib_deriv4(d, y, th)
length(d4)

# Nothing at this order touches the location.
d4$mu_mu_mu_mu

# Against a central difference of the third order.
eps <- 1e-5
rbind(analytic = d4$mu_mu_lambda_lambda,
      numeric = (distrib_deriv3(d, y, list(mu = 0, lambda = 2 + eps,
                                           alpha = 0.5))$mu_mu_lambda -
                 distrib_deriv3(d, y, list(mu = 0, lambda = 2 - eps,
                                           alpha = 0.5))$mu_mu_lambda) /
                (2 * eps))

# The pure-lambda component against one stencil on the log-density.
ld <- function(v) sum(distrib_pdf(d, y, list(mu = 0, lambda = v,
                                             alpha = 0.5), log = TRUE))
c(ours = sum(d4$lambda_lambda_lambda_lambda),
  stencil = numericals7::fd_derivative(ld, 2, 4L, h = 0.05))
}
\seealso{
\code{\link[=distrib_deriv3.EnetDistrib]{distrib_deriv3.EnetDistrib()}} for the order below,
\code{\link[=.enet_chain]{.enet_chain()}} for the assembly, and \code{\link[=distrib_deriv4]{distrib_deriv4()}} for the generic.
}
