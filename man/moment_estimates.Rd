% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/starting_values.R
\name{moment_estimates}
\alias{moment_estimates}
\title{Method-of-Moments Estimates, Family by Family}
\usage{
moment_estimates(distrib, y)
}
\arguments{
\item{distrib}{A univariate distribution, read for its \code{distrib_name} and
its parameter names.}

\item{y}{The response, a numeric vector, already filtered to finite values
by the caller.}
}
\value{
A named list of parameters on the parameter scale, one component per
entry of \code{distrib@params}, or \code{NULL} where this family has no entry.
}
\description{
Returns the parameters a family's own first two moments imply for a sample,
in closed form where the inversion has one, and \code{NULL} where this family has
no entry. \strong{37 of the 42 univariate families have one.}

A starting value should be an estimate. For most families the moment
estimate is one line: the sample mean and variance are set equal to the
family's own and the pair is inverted. \code{\link[=fit_distrib]{fit_distrib()}} then refines it by
maximum likelihood, and \pkg{statmodels7} takes the result as the intercept
of each equation.
}
\section{How the inversions are written and checked}{
Each is written against the family's own \code{\link[=mean]{mean()}} and \code{\link[=variance]{variance()}}, not from
memory, and the tests check it the same way: a family's moment estimate
applied to a large sample drawn from a known parameter must return that
parameter.
}

\section{Where a moment does not exist, and where the inversion is not closed}{
A family with no moments takes the robust analogue. The Cauchy uses the
median and half the interquartile range, which are its location and its
scale. Where the inversion needs a root, the standard approximation
stands in: the Weibull's shape from the coefficient of variation, the von
Mises's concentration from the mean resultant length. A starting value is
allowed to be approximate.

Five families have no entry and fall through to
\code{\link[=start_from_moments]{start_from_moments()}}'s reading of \code{params_interpretation}: \code{gengamma1},
\code{gengamma2}, \code{skewt}, \code{pseudohuber} and \code{enet}. Their inversions are neither
closed nor standard: two gamma-function ratios in two shapes for the
generalized gamma, a moment that does not identify the pair for the skew
\eqn{t}, no elementary moments at all for the elastic net.
}

\section{A fixed constant in the name}{
A family carrying one announces it there, as in \code{"beta-binomial [size=10]"},
so the bracketed part is stripped before the name is used as a lookup key.
Without that no beta-binomial of any size would match its own entry, and
silently: a missing key is a legal fallback.
}

\seealso{
\code{\link[=start_from_moments]{start_from_moments()}}, the only caller and the fallback;
\code{\link[=distrib_start]{distrib_start()}} for the generic; \code{\link[=mean]{mean()}} and \code{\link[=variance]{variance()}}, which the
inversions are written against.
}
\keyword{internal}
