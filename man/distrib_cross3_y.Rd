% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross3_derivatives.R
\name{distrib_cross3_y}
\alias{distrib_cross3_y}
\title{Mixed Third-Response Parameter Derivatives of the Log-Density}
\usage{
distrib_cross3_y(distrib, y, theta, scale = c("parameter", "link"), ...)
}
\arguments{
\item{distrib}{A distribution object inheriting from \code{continuous_distrib}.}

\item{y}{A numeric vector of observations.}

\item{theta}{A named list, or named numeric vector, of distribution
parameters. Each must have length 1 or \code{length(y)}.}

\item{scale}{Either \code{"parameter"} (default) for derivatives with respect to
the parameters \eqn{\theta} on their natural (constrained) scale, or
\code{"link"} for derivatives with respect to the unconstrained linear
predictors \eqn{\eta = g(\theta)} defined by \code{distrib@link_params}.
See \code{\link[=link_scale_derivatives]{link_scale_derivatives()}}.}

\item{...}{Passed to the method.}
}
\value{
A named list with one numeric vector per parameter, each as long as
\code{y}, keyed by \code{distrib@params}.
}
\description{
Computes \eqn{\partial^4 \ell / \partial y^3\, \partial \theta_i}, one
component per parameter, each a vector along \code{y}. It is
\code{\link[=distrib_deriv3_y]{distrib_deriv3_y()}} differentiated once in each parameter.
}
\section{What consumes it}{
A penalty \eqn{-\log f(D\beta;\theta)} has the Hessian
\eqn{S = -D'\mathrm{diag}(\ell^{(yy)})D} in the coefficients, whose
derivative along a direction \eqn{v} is
\eqn{-D'\mathrm{diag}(\ell^{(yyy)} \odot Dv)D}. The second derivative of a
marginal criterion in the hyperparameters reads how that matrix moves with
each hyperparameter, \eqn{-D'\mathrm{diag}(\partial_{\theta_i}\ell^{(yyy)}
\odot Dv)D}, and this is the vector placed on its diagonal.
}

\section{Where the numbers come from}{
For a family whose response enters only as \eqn{y - \mu} the response
derivative of order three is \eqn{-\partial^3\ell/\partial\mu^3}, so
\deqn{\frac{\partial^4\ell}{\partial y^3\,\partial\theta_i} =
  -\frac{\partial^4\ell}{\partial\mu^3\,\partial\theta_i},}
a component of \code{\link[=distrib_deriv4]{distrib_deriv4()}} with a sign. The fourteen location
families \code{\link[=distrib_deriv3_y]{distrib_deriv3_y()}} serves by the same identity are served this
way. Every other continuous family takes one central difference of
\code{\link[=distrib_deriv3_y]{distrib_deriv3_y()}} in each parameter, through \code{\link[=numerical_cross3_y]{numerical_cross3_y()}}.
}

\section{The link scale}{
The component for \eqn{\eta_i} is the parameter-scale component multiplied
by \eqn{h_i'(\eta_i)}: the response derivatives are untouched by a
reparametrization of \eqn{\theta}, so only the first-order diagonal chain
rule enters, as for \code{\link[=distrib_cross_y]{distrib_cross_y()}} and \code{\link[=distrib_cross2_y]{distrib_cross2_y()}}.
}

\section{Notation}{

\eqn{\ell} is the log-density of one observation, \eqn{y} the response,
\eqn{\theta_i} a distribution parameter, \eqn{\eta_i} its value on the
unconstrained scale and \eqn{h_i = g_i^{-1}} the inverse link carrying one
to the other. \eqn{\ell^{(yyy)}} is \eqn{\partial^3\ell/\partial y^3}.
}

\examples{
d <- student_t1_distrib()
y <- c(-1, 0, 2)
theta <- list(mu = 0.4, sigma = 1.3, nu = 6)
distrib_cross3_y(d, y, theta)

# Against a numerical derivative of the analytic third response derivative.
f <- function(v) distrib_deriv3_y(d, y[1], list(mu = 0.4, sigma = v, nu = 6))
numDeriv::grad(f, 1.3)

# A family whose response is not a location takes the difference.
distrib_cross3_y(gamma1_distrib(), c(0.5, 1, 2), list(mu = 1.5, phi = 0.8))

}
\seealso{
\code{\link[=distrib_deriv3_y]{distrib_deriv3_y()}} for the quantity being differentiated,
\code{\link[=distrib_cross2_y]{distrib_cross2_y()}} for the order below, and \code{\link[=numerical_cross3_y]{numerical_cross3_y()}} for
the fallback.
}
