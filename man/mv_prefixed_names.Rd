% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multivariate.R
\name{mv_prefixed_names}
\alias{mv_prefixed_names}
\title{Prefix a Parametrization's Free Names with the Matrix They Describe}
\usage{
mv_prefixed_names(free_names, inverted = FALSE)
}
\arguments{
\item{free_names}{The parametrization's own free names, a character vector.}

\item{inverted}{Logical of length 1. \code{TRUE} for a precision, giving
\code{"omega_"}; \code{FALSE}, the default, for a covariance or a scale matrix,
giving \code{"sigma_"}.}
}
\value{
A character vector as long as \code{free_names}.
}
\description{
Returns the matrix parametrization's free names with \code{"sigma_"} or
\code{"omega_"} in front, so that a printed parameter table says which matrix the
coordinate belongs to. A free value's name says how the matrix is BUILT, not
which matrix it is, so the same parametrization on the two sides of a model
would otherwise give two genuinely different models the same parameter
names.
}
\details{
The prefix is applied by the DISTRIBUTION. The parametrization does not
know which side of a model it has been handed to, so it cannot apply one.
}
\examples{
distributions7:::mv_prefixed_names(c("log_L1", "log_L2", "L2.1"))
distributions7:::mv_prefixed_names(c("log_L1", "L2.1"), inverted = TRUE)

# Which is what separates the two parametrizations of one law.
mvgaussian_distrib(2)@params
mvgaussian_distrib(2, omega = parameters7::log_cholesky(2))@params

}
\seealso{
\code{\link[=mvgaussian_distrib]{mvgaussian_distrib()}}, whose two forms this distinguishes, and
\code{\link[parameters7:log_cholesky]{parameters7::log_cholesky()}} for a source of free names.
}
\keyword{internal}
