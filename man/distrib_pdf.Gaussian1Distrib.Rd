% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaussian1_distrib.R
\name{distrib_pdf.Gaussian1Distrib}
\alias{distrib_pdf.Gaussian1Distrib}
\title{Gaussian Probability Density Function}
\arguments{
\item{distrib}{A \code{Gaussian1Distrib} object, from \code{\link[=gaussian1_distrib]{gaussian1_distrib()}}.}

\item{y}{A numeric vector of observations. Every real value is in the
support, so no value is rejected.}

\item{theta}{A named list with components \code{mu} and \code{sigma}, each a numeric
vector of length 1 or of the length of \code{y}. A component of length 1 is
recycled. \code{sigma} must be strictly positive; a zero or negative value
gives \code{NaN} with a warning from \code{\link[stats:Normal]{stats::dnorm()}}.}

\item{log}{Logical of length 1. When \code{TRUE} the log-density is returned.
Defaults to \code{FALSE}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of densities, of length
\code{max(length(y), length(mu), length(sigma))}, one value per observation.
}
\description{
Computes the Gaussian density
\deqn{f(y; \mu, \sigma) = \dfrac{1}{\sqrt{2\pi}\,\sigma} \exp\left\{-\dfrac{1}{2}\left(\dfrac{y-\mu}{\sigma}\right)^2\right\}}
by calling \code{\link[stats:Normal]{stats::dnorm()}} at \code{mean = mu} and \code{sd = sigma}, so the accuracy
and the underflow behavior are R's own. With \code{log = TRUE} the logarithm is
formed inside \code{dnorm()} and stays finite far into the tails, where the
density itself underflows to zero.
}
\examples{
d <- gaussian1_distrib()
y <- c(-1.2, 0.3, 2.5)

# The method is stats::dnorm at this parametrization.
all.equal(distrib_pdf(d, y, list(mu = 0.4, sigma = 1.5)),
          dnorm(y, mean = 0.4, sd = 1.5))

# A parameter may vary by observation, one value each.
distrib_pdf(d, y, list(mu = c(0, 1, 2), sigma = c(1, 1.5, 2)))

# In the far tail the density underflows and its logarithm does not.
distrib_pdf(d, 40, list(mu = 0, sigma = 1))
distrib_pdf(d, 40, list(mu = 0, sigma = 1), log = TRUE)
}
\seealso{
\code{\link[=distrib_cdf.Gaussian1Distrib]{distrib_cdf.Gaussian1Distrib()}} for the distribution function,
\code{\link[=distrib_gradient.Gaussian1Distrib]{distrib_gradient.Gaussian1Distrib()}} for the derivatives of the
log-density, \code{\link[=distrib_pdf]{distrib_pdf()}} for the generic and
\code{\link[=gaussian1_distrib]{gaussian1_distrib()}} for the family.
}
