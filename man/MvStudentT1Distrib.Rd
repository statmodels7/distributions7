% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mvstudent_t_distrib.R
\name{MvStudentT1Distrib}
\alias{MvStudentT1Distrib}
\alias{MvStudentT2Distrib}
\title{S7 Classes for the Two Multivariate Student t Parametrizations}
\usage{
MvStudentT1Distrib(
  distrib_name = character(0),
  dimension = character(0),
  bounds = integer(0),
  params = character(0),
  params_interpretation = character(0),
  n_params = integer(0),
  params_bounds = list(),
  link_params = list(),
  params_smooth = logical(0),
  n_dim = integer(0),
  param = parameters7::parameter(),
  inverted = logical(0)
)

MvStudentT2Distrib(
  distrib_name = character(0),
  dimension = character(0),
  bounds = integer(0),
  params = character(0),
  params_interpretation = character(0),
  n_params = integer(0),
  params_bounds = list(),
  link_params = list(),
  params_smooth = logical(0),
  n_dim = integer(0),
  param = parameters7::parameter(),
  inverted = logical(0)
)
}
\arguments{
\item{distrib_name}{A single character string specifying the name of the distribution (e.g., \code{"student t"}).}

\item{dimension}{A character string indicating the dimensionality (\code{"univariate"} or \code{"multivariate"}).}

\item{bounds}{A numeric vector of length 2 defining the overall support of the distribution \code{c(lower, upper)}.}

\item{params}{A character vector containing the names of the distribution parameters (e.g., \code{c("mu", "sigma")}).}

\item{params_interpretation}{A character vector (typically named) providing the statistical interpretation of each parameter (e.g., \code{c(mu = "location")}).}

\item{n_params}{A numeric value specifying the total number of parameters.}

\item{params_bounds}{A list of numeric vectors of length 2, specifying the valid mathematical domain for each individual parameter.}

\item{link_params}{A list of link function objects corresponding to each parameter, primarily used to map parameters to the unconstrained real line for optimization algorithms.}

\item{params_smooth}{An optional named logical vector flagging, for each parameter, whether the log-likelihood is differentiable with respect to it. Defaults to all \code{TRUE} (leave empty). Set an entry to \code{FALSE} for parameters at which the log-likelihood has a kink (e.g. the location of a Laplace distribution): the observed Hessian is then degenerate and the expected information must be obtained from the score variance rather than from \eqn{-\mathbb{E}[H]} (see \code{\link[=distrib_expected_hessian]{distrib_expected_hessian()}}).}

\item{n_dim}{The dimension \eqn{p} of one observation. A single positive
integer.}

\item{param}{A \pkg{parameters7} parametrization of the scale matrix or of
its inverse, according to \code{inverted}, inheriting from
\code{parameters7::parameter}. Its \code{n_free} free values are flattened into
scalar parameters of the distribution.}

\item{inverted}{Logical of length 1. \code{TRUE} when \code{param} carries the
inverse scale matrix \eqn{\Sigma^{-1}} and \code{FALSE} when it carries the
scale matrix \eqn{\Sigma}. Nothing but the sign of the log-determinant and
one matrix inversion depends on it; the law is the same either way, and
neither matrix is a moment of the response.}
}
\value{
An object of class \code{MvStudentT1Distrib} or \code{MvStudentT2Distrib}, a
subclass of \link{MvStudentTDistrib} adding no properties of its own.
}
\description{
\code{MvStudentT1Distrib} is the family whose matrix parametrization is the
\strong{scale matrix} \eqn{\Sigma} and \code{MvStudentT2Distrib} the family whose
parametrization is its \strong{inverse} \eqn{\Sigma^{-1}}. Both inherit from
\link{MvStudentTDistrib}, which carries every method: the two differ in what
their free values describe, not in how anything is computed.
}
\details{
Neither matrix is a covariance or a precision of the response, and the
difference is a factor in \eqn{\nu} that \code{\link[=mvstudent_t1_distrib]{mvstudent_t1_distrib()}} writes
out. The numbering is the package's convention, the one that separates
\code{\link[=mvgaussian1_distrib]{mvgaussian1_distrib()}} from \code{\link[=mvgaussian2_distrib]{mvgaussian2_distrib()}} and
\code{\link[=gaussian1_distrib]{gaussian1_distrib()}} from \code{\link[=gaussian3_distrib]{gaussian3_distrib()}}: a parametrization decides
what a linear predictor acts on, so it gets a name.
}
\examples{
c(scale = S7::S7_inherits(mvstudent_t1_distrib(2), MvStudentT1Distrib),
  inverse = S7::S7_inherits(mvstudent_t2_distrib(2), MvStudentT2Distrib))

}
\seealso{
\code{\link[=mvstudent_t1_distrib]{mvstudent_t1_distrib()}} and \code{\link[=mvstudent_t2_distrib]{mvstudent_t2_distrib()}}, the
constructors, and \link{MvStudentTDistrib} for the properties and the methods.
}
