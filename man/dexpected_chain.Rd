% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dexpected_reparam.R
\name{dexpected_chain}
\alias{dexpected_chain}
\title{The Chain Rule for the Expected Information's Derivatives}
\usage{
dexpected_chain(Pp, Pn, E, d1, d2, maps, order, n)
}
\arguments{
\item{Pp, Pn}{The parent's parameter names and the new ones.}

\item{E, d1, d2}{Named lists keyed as \code{\link[=hess_names]{hess_names()}}, \code{\link[=dexpected_names]{dexpected_names()}}
and \code{\link[=d2expected_names]{d2expected_names()}} on the parent's parameters; \code{d2} is read only at
order 2.}

\item{maps}{One keyed table per parent parameter, as \code{\link[=reparam_tables]{reparam_tables()}}
returns them: the key is the sorted, comma-joined indices of the new
parameters differentiated, and a missing key is an exact zero.}

\item{order}{\code{1L} or \code{2L}.}

\item{n}{The number of observations, to which every component is recycled.}
}
\value{
A named list keyed as \code{\link[=dexpected_names]{dexpected_names()}} or \code{\link[=d2expected_names]{d2expected_names()}}
on the new parameters.
}
\description{
The arithmetic of \code{\link[=reparam_dexpected]{reparam_dexpected()}}, given the parent's quantities
rather than the parent: \eqn{E}, \eqn{\partial E} and, at order 2,
\eqn{\partial^2 E} on the parent's scale, and the map's partials as keyed
tables. It serves a reparametrized family and any family written as a map
of another's coordinates, such as \code{\link[=betabinom1_distrib]{betabinom1_distrib()}} over the shapes.
}
\seealso{
\code{\link[=reparam_dexpected]{reparam_dexpected()}}
}
\keyword{internal}
