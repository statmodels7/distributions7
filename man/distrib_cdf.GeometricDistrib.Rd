% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometric_distrib.R
\name{distrib_cdf.GeometricDistrib}
\alias{distrib_cdf.GeometricDistrib}
\title{Geometric Cumulative Distribution Function}
\arguments{
\item{distrib}{A \code{GeometricDistrib} object, from \code{\link[=geometric_distrib]{geometric_distrib()}}.}

\item{q}{A numeric vector of quantiles. A non-integer is floored, and a
negative value gives 0.}

\item{theta}{A named list with the single component \code{mu}, a numeric vector
of length 1 or of the length of \code{q}. \code{mu} must be strictly positive.}

\item{lower.tail}{Logical of length 1. When \code{TRUE}, the default,
probabilities are \eqn{P(Y \le q)}; when \code{FALSE} they are the survival
function \eqn{P(Y > q)}, exact far into the tail.}

\item{log.p}{Logical of length 1. When \code{TRUE} the logarithm of the
probability is returned. Defaults to \code{FALSE}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector of probabilities in \eqn{[0, 1]}, of length
\code{max(length(q), length(mu))}.
}
\description{
Computes the geometric distribution function
\deqn{F(q; \mu) = 1 - \left(\dfrac{\mu}{1+\mu}\right)^{\lfloor q \rfloor + 1}}
by calling \code{\link[stats:Geometric]{stats::pgeom()}} at \code{prob = 1/(1+mu)}. The survival function is
exactly \eqn{(\mu/(1+\mu))^{q+1}}, a geometric decay, and
\code{lower.tail = FALSE} returns it without forming the difference.
}
\examples{
d <- geometric_distrib()
th <- list(mu = 3)

# The method is stats::pgeom at prob = 1/(1+mu).
all.equal(distrib_cdf(d, c(0, 2, 7), th), pgeom(c(0, 2, 7), prob = 1 / 4))

# The survival function is a geometric decay, written out.
all.equal(distrib_cdf(d, c(0, 2, 7), th, lower.tail = FALSE),
          (3 / 4)^(c(0, 2, 7) + 1))

# Memoryless: the chance of at least one more failure does not depend on
# how many have already been seen.
vapply(c(0, 2, 10, 50), function(s)
  distrib_cdf(d, s + 1, th, lower.tail = FALSE) /
    distrib_cdf(d, s, th, lower.tail = FALSE), numeric(1))
}
\seealso{
\code{\link[=distrib_quantile.GeometricDistrib]{distrib_quantile.GeometricDistrib()}} for the generalized inverse,
\code{\link[=distrib_pdf.GeometricDistrib]{distrib_pdf.GeometricDistrib()}} for the mass, and \code{\link[=distrib_cdf]{distrib_cdf()}} for the
generic.
}
