% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdf_derivatives_families.R
\name{distrib_hess_cdf.ReparamContinuousDistrib}
\alias{distrib_hess_cdf.ReparamContinuousDistrib}
\title{Log-CDF Hessian of a Reparametrized Distribution}
\arguments{
\item{distrib}{A \code{ReparamContinuousDistrib}, from \code{\link[=reparametrize]{reparametrize()}}.}

\item{q}{A numeric vector of quantiles.}

\item{theta}{A named list of the new parameters, on the new parameter scale.}

\item{lower.tail}{Is the lower tail wanted? A single logical, \code{TRUE} by
default.}

\item{log}{Are derivatives of the log probability wanted? A single logical,
\code{TRUE} by default.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A named list of numeric vectors keyed as \code{\link[=hess_names]{hess_names()}}, each the
length of \code{q} recycled against \code{theta}. The gradient is not returned
alongside.
}
\description{
The second-order chain rule on the parent's cdf derivatives, through
\code{\link[=mapped_cdf_deriv]{mapped_cdf_deriv()}}:
\eqn{\partial^2 F/\partial\psi_a\partial\psi_b
     = \sum_{k,l} F_{kl} h^k_a h^l_b + \sum_k F_k h^k_{ab}}. The second sum
is the one a first-order chain does not have, and it is why the map's second
partials are needed as well as its first.
}
\details{
The gate asks the parent for exactness at \strong{both} orders. A parent with a
closed gradient and a differenced Hessian sends this whole method to
\code{\link[=numerical_cdf_deriv]{numerical_cdf_deriv()}}, because carrying an exact first order onto an
approximate second would not make the second exact.
}
\examples{
d <- reparametrize(
  gaussian1_distrib(),
  map = function(psi) list(mu = psi$mu, sigma = sqrt(psi$sigma2)),
  params = c("mu", "sigma2"),
  bounds = list(mu = c(-Inf, Inf), sigma2 = c(0, Inf)),
  links = list(mu = linkfunctions7::identity_link(),
               sigma2 = linkfunctions7::log_link())
)
q <- c(-1, 0.5, 2)
th <- list(mu = 0.3, sigma2 = 1.44)

# Against a central difference of the reparametrized cdf.
exact <- distrib_hess_cdf(d, q, th, log = FALSE)
fd <- numerical_cdf_deriv(d, q, th, order = 2)
max(abs(unlist(exact[names(fd)]) - unlist(fd)))

}
\seealso{
\code{\link[=distrib_grad_cdf.ReparamContinuousDistrib]{distrib_grad_cdf.ReparamContinuousDistrib()}} for the first order;
\code{\link[=chain_cdf_deriv]{chain_cdf_deriv()}} for the identity; \code{\link[=reparametrize]{reparametrize()}}.
}
\keyword{internal}
