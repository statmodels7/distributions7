% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mv_plot.R
\name{mv_pairs_panels}
\alias{mv_pairs_panels}
\title{Draw the Panel Matrix of a Multivariate Density}
\usage{
mv_pairs_panels(
  d,
  theta,
  which,
  n_grid,
  col_fit,
  data,
  col_data = "#4682B4",
  ...
)
}
\arguments{
\item{d}{A \code{\link[=multivariate_distrib]{multivariate_distrib()}} object.}

\item{theta}{A named list of parameters, already aligned.}

\item{which}{The coordinates to show, or \code{NULL} for all.}

\item{n_grid}{Points per axis.}

\item{col_fit}{Color of the fitted density.}

\item{data}{An \eqn{n \times p} matrix of observations, or \code{NULL}.}

\item{col_data}{Color of the observed summary.}

\item{...}{Unused.}
}
\value{
Invisibly \code{NULL}.
}
\description{
The common engine of \code{\link[=plot.multivariate_distrib]{plot.multivariate_distrib()}} and the
multivariate branch of \code{\link[=plot.distrib_fit]{plot.distrib_fit()}}: one panel per pair of
coordinates, the marginal densities on the diagonal.
}
\details{
When \code{data} is supplied the diagonal also carries a kernel density
estimate of the observed coordinate and the off-diagonal panels carry the
observations, so that the fitted shape and the sample are read against each
other in the same frame. The kernel estimate is the comparison that does not
assume the model: it is what the data say without the family's help.
}
\keyword{internal}
