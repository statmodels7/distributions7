% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moments.R
\name{skewness.Pig2Distrib}
\alias{skewness.Pig2Distrib}
\title{Skewness of the Orthogonal Poisson-Inverse Gaussian Distribution}
\arguments{
\item{x}{A \code{Pig2Distrib}, from \code{\link[=pig2_distrib]{pig2_distrib()}}.}

\item{theta}{A named list with components \code{mu} (positive) and \code{alpha}
(positive), each a numeric vector of length 1 or \code{n}.}

\item{...}{Unused, and accepted so that the signature matches the generic's.}
}
\value{
A numeric vector, of length
\code{max(length(theta$mu), length(theta$alpha))}, positive throughout.
}
\description{
The cumulant ratio \eqn{\gamma_1 = \kappa_3/\kappa_2^{3/2}} of
\code{\link[=skewness.Pig1Distrib]{skewness.Pig1Distrib()}}, read at the dispersion
\eqn{\sigma = } \code{\link[=pig2_sigma]{pig2_sigma()}}\verb{(mu, alpha)}. A standardized moment is a
property of the law, so it agrees with the other parametrization once the
mapping is applied.
}
\section{Notation}{

\eqn{\mu > 0} is the mean, \eqn{\alpha > 0} the orthogonal dispersion and
\eqn{\kappa_k} the \eqn{k}-th cumulant.
}

\examples{
d <- pig2_distrib()

# The two parametrizations agree once the mapping is applied.
s <- distributions7:::pig2_sigma(3, 1)
all.equal(skewness(d, list(mu = 3, alpha = 1)),
          skewness(pig1_distrib(), list(mu = 3, sigma = s)))

}
\seealso{
\code{\link[=kurtosis.Pig2Distrib]{kurtosis.Pig2Distrib()}}, from the fourth cumulant;
\code{\link[=pig2_sigma]{pig2_sigma()}}; \code{\link[=skewness.Pig1Distrib]{skewness.Pig1Distrib()}}; \code{\link[=pig2_distrib]{pig2_distrib()}}.
}
\keyword{internal}
