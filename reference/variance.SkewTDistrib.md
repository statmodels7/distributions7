# Variance of the Skew t Distribution

Closed form for \\\nu \> 2\\: \\\operatorname{Var}(Y) =
\sigma^2\\\nu/(\nu-2) - \mu_z^2\\\\, with \\\mu_z = \delta b\_\nu\\ the
mean of the standardized variable. At \\\nu \le 2\\ the value is `NaN`,
the second moment not existing there. Two effects push in opposite
directions: the \\t\\ factor \\\nu/(\nu-2)\\ inflates the variance above
the scale, and the skewing term \\\mu_z^2\\ pulls it back.

## Arguments

- x:

  A `SkewTDistrib`, from
  [`skewt_distrib()`](https://statmodels7.github.io/distributions7/reference/skewt_distrib.md).

- theta:

  A named list with components `mu`, `sigma` (positive), `alpha` (any
  sign) and `nu` (positive), each a numeric vector of length 1 or `n`.
  Settings with \\\nu \le 2\\ give `NaN`.

- ...:

  Unused, and accepted so that the signature matches the generic's.

## Value

A numeric vector of variances, of the length the recycled parameters
imply, `NaN` wherever \\\nu \le 2\\. The scale, `alpha` and `nu` enter
the value; the location does not.

## Notation

\\\sigma \> 0\\ is the scale, \\\alpha\\ the shape, \\\nu\\ the degrees
of freedom, \\\delta = \alpha/\sqrt{1+\alpha^2}\\ and \\b\_\nu =
\sqrt{\nu/\pi}\\\Gamma\\(\nu-1)/2\\/\Gamma(\nu/2)\\.

## References

Azzalini, A. and Capitanio, A. (2003). Distributions generated by
perturbation of symmetry with emphasis on a multivariate skew t
distribution. *Journal of the Royal Statistical Society Series B*
**65**, 367-389.

## See also

[`mean.SkewTDistrib()`](https://statmodels7.github.io/distributions7/reference/mean.SkewTDistrib.md),
whose threshold is 1;
[`skewness.SkewTDistrib()`](https://statmodels7.github.io/distributions7/reference/skewness.SkewTDistrib.md),
whose threshold is 3;
[`skewt_moment_pieces()`](https://statmodels7.github.io/distributions7/reference/skewt_moment_pieces.md),
[`skewt_distrib()`](https://statmodels7.github.io/distributions7/reference/skewt_distrib.md).

## Examples

``` r
d <- skewt_distrib()

# At shape zero this is the Student t's nu / (nu - 2).
all.equal(variance(d, list(mu = 0, sigma = 1, alpha = 0, nu = 5)), 5 / 3)
#> [1] TRUE

# It does not exist at or below two degrees of freedom.
variance(d, list(mu = 0, sigma = 1, alpha = 2, nu = c(2, 5)))
#> [1]       NaN 0.9461605
```
