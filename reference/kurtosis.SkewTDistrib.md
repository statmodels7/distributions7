# Excess Kurtosis of the Skew t Distribution

Closed form for \\\nu \> 4\\, from Azzalini and Capitanio (2003):
\$\$\gamma_2 = \frac{1}{\sigma_z^{4}}
\left\\\frac{3\nu^2}{(\nu-2)(\nu-4)} -
\frac{4\mu_z^2\nu(3-\delta^2)}{\nu-3} + \frac{6\mu_z^2\nu}{\nu-2} -
3\mu_z^4\right\\ - 3,\$\$ and `NaN` at \\\nu \le 4\\. The threshold is
the highest of the four moments, the fourth being the one that fails
first as the tails grow heavy.

## Arguments

- x:

  A `SkewTDistrib`, from
  [`skewt_distrib()`](https://statmodels7.github.io/distributions7/reference/skewt_distrib.md).

- theta:

  A named list with components `mu`, `sigma` (positive), `alpha` (any
  sign) and `nu` (positive), each a numeric vector of length 1 or `n`.
  Only `alpha` and `nu` enter the value; settings with \\\nu \le 4\\
  give `NaN`.

- ...:

  Unused, and accepted so that the signature matches the generic's.

## Value

A numeric vector of excess kurtoses, of the length the recycled
parameters imply, `NaN` wherever \\\nu \le 4\\. Only `alpha` and `nu`
enter the value.

## Notation

\\\delta = \alpha/\sqrt{1+\alpha^2}\\, \\\mu_z = \delta b\_\nu\\ and
\\\sigma_z^2 = \nu/(\nu-2) - \mu_z^2\\ are the standardized mean and
variance from
[`skewt_moment_pieces()`](https://statmodels7.github.io/distributions7/reference/skewt_moment_pieces.md).

## References

Azzalini, A. and Capitanio, A. (2003). Distributions generated by
perturbation of symmetry with emphasis on a multivariate skew t
distribution. *Journal of the Royal Statistical Society Series B*
**65**, 367-389.

## See also

[`skewness.SkewTDistrib()`](https://statmodels7.github.io/distributions7/reference/skewness.SkewTDistrib.md),
whose threshold is 3;
[`kurtosis.SkewNormal1Distrib()`](https://statmodels7.github.io/distributions7/reference/kurtosis.SkewNormal1Distrib.md),
the bounded limit;
[`skewt_moment_pieces()`](https://statmodels7.github.io/distributions7/reference/skewt_moment_pieces.md),
[`skewt_distrib()`](https://statmodels7.github.io/distributions7/reference/skewt_distrib.md).

## Examples

``` r
d <- skewt_distrib()

# At shape zero this is the Student t's 6 / (nu - 4).
kurtosis(d, list(mu = 0, sigma = 1, alpha = 0, nu = c(4, 5, 10)))
#> [1] NaN   6   1

# As nu grows it approaches the skew normal's value at the same shape.
c(skewt = kurtosis(d, list(mu = 0, sigma = 1, alpha = 3, nu = 1e6)),
  skewnormal = kurtosis(skewnormal1_distrib(),
                        list(mu = 0, sigma = 1, alpha = 3)))
#>      skewt skewnormal 
#>  0.5097811  0.5097701 
```
