# Print Method for `distrib` Objects

Custom S7 print method for objects inheriting from `distrib`.

## Arguments

- x:

  An object inheriting from class `"distrib"`.

- ...:

  Additional arguments (currently unused).

## Value

The object `x` invisibly.
