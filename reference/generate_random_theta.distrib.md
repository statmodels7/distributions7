# Generate Random Parameters for `distrib` Objects

Generates a named list of sensible random parameters based on the
mathematical domain (`params_bounds`) of the given distribution.

## Arguments

- distrib:

  An object inheriting from class `"distrib"`.

- ...:

  Additional arguments (currently unused).

## Value

A named list of randomly generated parameters.
