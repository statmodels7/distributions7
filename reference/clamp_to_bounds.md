# Move a Starting Value Strictly Inside the Parameter Bounds

Move a Starting Value Strictly Inside the Parameter Bounds

## Usage

``` r
clamp_to_bounds(theta, distrib)
```

## Arguments

- theta:

  A named list.

- distrib:

  The distribution whose bounds apply.

## Value

`theta`, each element strictly inside its own bounds.
