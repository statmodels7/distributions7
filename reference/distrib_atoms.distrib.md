# Default Atoms: None

Ordinary distributions have nothing to declare here: a continuous one
has no atoms and a discrete one is made of nothing else. Only a mixed
distribution registers something more specific.

## Arguments

- distrib:

  A `distrib` object.

- theta:

  A named list of parameters.

## Value

A list with empty `y` and `p`.
