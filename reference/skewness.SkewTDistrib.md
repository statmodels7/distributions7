# Skewness of the Skew t Distribution

Closed form for \\\nu \> 3\\, from Azzalini and Capitanio (2003):
\$\$\gamma_1 = \frac{\mu_z}{\sigma_z^{3}}
\left\\\frac{\nu(3-\delta^2)}{\nu-3} - \frac{3\nu}{\nu-2} +
2\mu_z^2\right\\,\$\$ and `NaN` at \\\nu \le 3\\. Unlike the skew
normal's, this skewness is unbounded: heavy tails supply asymmetry the
Gaussian-tailed family cannot reach at any shape.

## Arguments

- x:

  A `SkewTDistrib`, from
  [`skewt_distrib()`](https://statmodels7.github.io/distributions7/reference/skewt_distrib.md).

- theta:

  A named list with components `mu`, `sigma` (positive), `alpha` (any
  sign) and `nu` (positive), each a numeric vector of length 1 or `n`.
  Only `alpha` and `nu` enter the value; settings with \\\nu \le 3\\
  give `NaN`.

- ...:

  Unused, and accepted so that the signature matches the generic's.

## Value

A numeric vector, of the length the recycled parameters imply, `NaN`
wherever \\\nu \le 3\\. Only `alpha` and `nu` enter the value.

## Notation

\\\delta = \alpha/\sqrt{1+\alpha^2}\\, \\\mu_z = \delta b\_\nu\\ and
\\\sigma_z^2 = \nu/(\nu-2) - \mu_z^2\\ are the standardized mean and
variance from
[`skewt_moment_pieces()`](https://statmodels7.github.io/distributions7/reference/skewt_moment_pieces.md).
Neither the location nor the scale enters a standardized moment.

## References

Azzalini, A. and Capitanio, A. (2003). Distributions generated by
perturbation of symmetry with emphasis on a multivariate skew t
distribution. *Journal of the Royal Statistical Society Series B*
**65**, 367-389.

## See also

[`kurtosis.SkewTDistrib()`](https://statmodels7.github.io/distributions7/reference/kurtosis.SkewTDistrib.md),
whose threshold is 4;
[`skewness.SkewNormal1Distrib()`](https://statmodels7.github.io/distributions7/reference/skewness.SkewNormal1Distrib.md),
the bounded limit;
[`skewt_moment_pieces()`](https://statmodels7.github.io/distributions7/reference/skewt_moment_pieces.md),
[`skewt_distrib()`](https://statmodels7.github.io/distributions7/reference/skewt_distrib.md).

## Examples

``` r
d <- skewt_distrib()

# Undefined at or below three degrees of freedom, then large and falling.
round(skewness(d, list(mu = 0, sigma = 1, alpha = 2, nu = c(3, 4, 10))), 4)
#> [1]    NaN 2.9938 0.8658

# As nu grows it approaches the skew normal's value at the same shape.
c(skewt = skewness(d, list(mu = 0, sigma = 1, alpha = 3, nu = 1e6)),
  skewnormal = skewness(skewnormal1_distrib(),
                        list(mu = 0, sigma = 1, alpha = 3)))
#>      skewt skewnormal 
#>  0.6670269  0.6670236 
```
